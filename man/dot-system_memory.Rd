% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-memory.R
\name{.system_memory}
\alias{.system_memory}
\title{Free and total physical memory, in bytes}
\usage{
.system_memory()
}
\value{
\code{list(total, available, source)}, bytes, \code{NA} when unknown.
}
\description{
macOS has no \code{MemAvailable}. The closest honest approximation is the
page classes the kernel can hand out without swapping -- free,
inactive, speculative and purgeable -- which is what \code{vm_stat} reports
and what this reads. Linux states \code{MemAvailable} directly and it is
the better number, so it is used as-is where present.
}
\details{
Every path is allowed to fail. A machine this does not recognise
returns \code{NA} availability, which downgrades the guard to silence
rather than to a wrong answer.
}
\keyword{internal}
