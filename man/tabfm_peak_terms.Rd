% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabfm.R
\name{tabfm_peak_terms}
\alias{tabfm_peak_terms}
\title{TabFM's peak-memory shapes}
\usage{
tabfm_peak_terms(n_context, n_query, n_features, opts, config)
}
\arguments{
\item{n_context, n_query, n_features}{Dimensions; \code{n_query} should
already be the resident chunk (see \code{\link[=.resident_query]{.resident_query()}}).}

\item{opts}{Resolved predictor arguments.}

\item{config}{The model config.}
}
\description{
The same three-stage shape as TabICL with two differences that both
cost memory. The cell embedder gives every \emph{cell} its own token rather
than one per group of three, so the widest activation is \verb{rows x columns x embed_dim} -- three times TabICL's at the same dimensions --
and the 1.6 B float32 parameters are 6.4 GB resident before a single
activation exists.
}
\details{
Two knobs move real bytes here and are honoured: \code{max_num_features}
caps the columns each member sees, and \code{max_num_rows} subsamples its
context.
}
\keyword{internal}
