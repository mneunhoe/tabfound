% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-mitra.R
\name{mitra_classifier}
\alias{mitra_classifier}
\title{Build the Mitra classifier predictor}
\usage{
mitra_classifier(
  ctx,
  n_estimators = 1L,
  random_mirror_x = TRUE,
  random_state = 42L,
  predict_chunk_size = 1024L,
  kv_cache = FALSE,
  save_peak_memory_factor = NULL
)
}
\arguments{
\item{ctx}{Loaded-model context from \code{\link[=load_backend_model]{load_backend_model()}}.}

\item{n_estimators}{Number of ensemble members. AutoGluon's default is
1; more members only help if \code{random_mirror_x} is on, since that is
the only thing distinguishing them.}

\item{random_mirror_x}{Flip a random sign per column, per member.}

\item{random_state}{Seed for the sign flips, or \code{NULL}. The reference
draws these from NumPy's \emph{global} generator and never seeds it, so
its own flips differ run to run; a seed is offered here because an R
user can reasonably expect reproducibility.}

\item{predict_chunk_size}{Max test rows per forward pass.}

\item{kv_cache}{Condition on the support set once per \code{predict()} call
and reuse that across every chunk and ensemble member, instead of
re-encoding the whole support set for each. Off by default, and the
memory it costs is the reason -- see \code{\link[=mitra_kv_cache]{mitra_kv_cache()}}. It answers
the same question an uncached pass does: the support half of this
architecture never looks at a query row.}

\item{save_peak_memory_factor}{Integer, or \code{NULL} (default) to
disable. Splits each of a layer's four sublayers into this many
chunks of rows. Every one of them is already independent across
rows, so the output is bit-identical and the only cost is loop
overhead. This is the knob Mitra most needs: it is the one backend
that attends across rows \emph{and} columns, so its activation carries
every row at full embedding width on both axes and is the steepest
in the package.}
}
\description{
Reproduces \code{MitraClassifier} minus fine-tuning: each member's
preprocessor imputes missing values with the training column means,
drops constant columns and applies its own random sign flips; each
member's logits are softmaxed and the probabilities averaged.
}
\details{
The imputation is the substantive part. Mitra does not propagate
\code{NaN}, it absorbs it — one missing value makes a column's quantiles
all-\code{NaN}, every value then buckets to zero, and the zero-variance
guard flattens the column. The output is finite and the feature has
silently vanished. The reference never sees that because it imputes
first, and now neither does this backend.
}
\keyword{internal}
