% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn3.R
\name{tabpfn3_sdpa}
\alias{tabpfn3_sdpa}
\title{Scaled dot-product attention on \verb{(B, S, H, D)} tensors}
\usage{
tabpfn3_sdpa(q, k, v, scaling = NULL)
}
\arguments{
\item{q}{\verb{(B, S, H, D)}; @param k,v \verb{(B, J, Hkv, D)}.}

\item{scaling}{Optional \code{\link[=tabpfn3_softmax_scaling_mlp]{tabpfn3_softmax_scaling_mlp()}}, applied to the
queries before the attention with \code{J} as its length argument.}
}
\description{
The reference keeps queries, keys and values head-last and permutes
inside its own \code{scaled_dot_product_attention} wrapper; this does the
same so the shapes in the callers line up with the reference's suffix
notation one for one.
}
\details{
When there are fewer key/value heads than query heads the keys and
values are repeated. Torch only has a fused grouped-query kernel for
half precision, so the reference falls back to \code{repeat_interleave} in
float32 -- which is what this reproduces, and is exact either way.
}
\keyword{internal}
