% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{fit_preprocessing_pipeline}
\alias{fit_preprocessing_pipeline}
\alias{transform_preprocessing_pipeline}
\title{The wrappers' shared preprocessing pipeline}
\usage{
fit_preprocessing_pipeline(
  X_train,
  normalization_method = "power",
  outlier_threshold = 4,
  quantile_subsample = NULL
)

transform_preprocessing_pipeline(X, fit)
}
\arguments{
\item{X_train}{Numeric matrix.}

\item{normalization_method}{One of \code{"none"}, \code{"power"}, \code{"quantile"},
\code{"quantile_rtdl"}, \code{"robust"}.}

\item{outlier_threshold}{Passed to \code{\link[=fit_outlier_remover]{fit_outlier_remover()}}.}

\item{quantile_subsample}{Row cap for the quantile normalisers; see
\code{\link[=fit_sk_quantile_transformer]{fit_sk_quantile_transformer()}}.}

\item{X}{Numeric matrix.}

\item{fit}{Result of \code{\link[=fit_preprocessing_pipeline]{fit_preprocessing_pipeline()}}.}
}
\description{
Scale, normalise, clip — in that order, and the order matters: the
normaliser is fitted on already-standardised data, and the outlier
bounds on already-normalised data.
}
\details{
TabICL and TabFM ship byte-identical copies of this class, so one
implementation serves both. What differs between them is only which
\code{normalization_method} each ensemble member is handed.

The fitted training output is cached in \code{X_transformed}, exactly as
the reference caches \code{X_transformed_} — every ensemble member sharing
a normalisation method reuses it rather than re-running the pipeline.
}
\keyword{internal}
