% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi-interop.R
\name{as_amelia}
\alias{as_amelia}
\alias{as_amelia.tabfound_mi}
\title{Convert to an Amelia \code{amelia} object}
\usage{
as_amelia(x, ...)

\method{as_amelia}{tabfound_mi}(x, ...)
}
\arguments{
\item{x}{A \code{tabfound_mi} object from \code{\link[=tabfound_impute]{tabfound_impute()}}.}

\item{...}{Unused.}
}
\value{
An object of class \code{amelia}.
}
\description{
Repackages imputations the way \code{Amelia::amelia()} returns them, so
anything that consumes one -- most usefully \code{Amelia::mi.meld()} for
pooling estimates and standard errors -- takes this directly.
}
\details{
The EM-specific slots (\code{theta}, \code{mu}, \code{covMatrices}) are \code{NULL}: they
describe a multivariate-normal model that was never fitted here, and
filling them with plausible-looking numbers would be worse than
leaving them empty. Amelia's diagnostics that read them
(\code{overimpute()}, \code{disperse()}, \code{compare.density()} in part) will not
work; \code{mi.meld()}, \code{missmap()} and the imputations themselves will.
}
\examples{
\dontrun{
amp <- as_amelia(imp)
fits <- lapply(amp$imputations, function(d) lm(Ozone ~ Wind + Temp, data = d))
b  <- t(sapply(fits, coef))
se <- t(sapply(fits, function(f) summary(f)$coefficients[, 2]))
Amelia::mi.meld(q = b, se = se)
}
}
