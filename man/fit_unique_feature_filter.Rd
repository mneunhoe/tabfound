% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{fit_unique_feature_filter}
\alias{fit_unique_feature_filter}
\alias{transform_unique_feature_filter}
\title{Which columns survive the unique-value filter}
\usage{
fit_unique_feature_filter(X_train, threshold = 1L)

transform_unique_feature_filter(X, fit)
}
\arguments{
\item{X_train}{Numeric matrix.}

\item{threshold}{Columns with at most this many distinct values go.}

\item{X}{Numeric matrix.}

\item{fit}{Result of \code{\link[=fit_unique_feature_filter]{fit_unique_feature_filter()}}.}
}
\value{
A list with the logical \code{keep} mask and \code{n_features_out}.
}
\description{
A column with \code{threshold} or fewer distinct values is dropped. Two
details are load-bearing:
}
\details{
\itemize{
\item \code{np.unique} collapses all \code{NaN}s into a single entry (numpy >= 1.21),
so a column of one value plus missings has \emph{two} distinct values and
survives. Counting \code{NA} as one category here matches that.
\item With \code{n_samples <= threshold} nothing is dropped at all. With that
few rows a column can look constant by accident, and the reference
would rather keep a useless column than lose a useful one.
}
}
\keyword{internal}
