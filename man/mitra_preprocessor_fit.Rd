% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-ensemble.R
\name{mitra_preprocessor_fit}
\alias{mitra_preprocessor_fit}
\alias{mitra_preprocessor_transform_X}
\alias{mitra_preprocessor_transform_y}
\alias{mitra_preprocessor_invert_y}
\title{Fit Mitra's preprocessor}
\usage{
mitra_preprocessor_fit(
  X,
  y,
  task = "classification",
  random_mirror_x = TRUE,
  random_mirror_regression = TRUE,
  seed = NULL
)

mitra_preprocessor_transform_X(X, fit)

mitra_preprocessor_transform_y(y, fit)

mitra_preprocessor_invert_y(pred, fit)
}
\arguments{
\item{X}{Numeric matrix of training predictors.}

\item{y}{Targets.}

\item{task}{\code{"classification"} or \code{"regression"}.}

\item{random_mirror_x}{Flip a random sign per column.}

\item{random_mirror_regression}{Possibly replace \code{y} with \code{1 - y}.}

\item{seed}{Integer seed for the mirrors, or \code{NULL}. The reference
draws these from NumPy's \emph{global} generator and never seeds it, so
its mirrors are not reproducible even run to run; a seed is offered
here because an R user can reasonably expect \code{set.seed()} to work.}

\item{fit}{Result of \code{\link[=mitra_preprocessor_fit]{mitra_preprocessor_fit()}}.}

\item{pred}{Numeric vector of predictions in the model's target space.}
}
\description{
Much the smallest of the three, and the only one that is not an
ensemble generator: \code{MitraClassifier} defaults to \code{n_estimators = 1},
and what diversity it has comes from each member drawing its own
random sign flips rather than from a designed set of views.
}
\details{
With AutoGluon's own defaults (\code{use_quantile_transformer = False},
\code{use_feature_count_scaling = False}, \code{use_random_transforms = False},
\code{shuffle_classes = False}, \code{shuffle_features = False}) the pipeline is:
impute missing values with the training column means, drop constant
columns, flip a random per-column sign, and map a regression target to
\verb{[0, 1]} — possibly mirrored.

The imputation is why this matters more than its size suggests. Mitra
does not propagate \code{NaN}, it \emph{absorbs} it: one missing value makes a
column's quantiles all-\code{NaN}, every value buckets to zero, and the
column silently vanishes. The reference never hits that because it
imputes first. Until now this port did not.
}
\keyword{internal}
