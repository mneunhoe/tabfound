% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-pipelines.R
\name{build_member_kv_caches}
\alias{build_member_kv_caches}
\title{Build one KV cache per ensemble member}
\usage{
build_member_kv_caches(
  net,
  col_emb,
  X_train,
  y_train,
  configs,
  member_y,
  device = "cpu",
  categorical_features = integer(),
  save_peak_memory_factor = NULL,
  row_chunk_size = NA_integer_,
  col_chunk_size = NA_integer_,
  pipelines = NULL
)
}
\arguments{
\item{net, col_emb}{Loaded network and column embeddings.}

\item{X_train}{Raw training predictors.}

\item{y_train}{Raw training target, in whatever form \code{member_y} expects.}

\item{configs}{Member specs.}

\item{member_y}{Function \verb{(cfg, y_train)} returning the target this
member conditions on -- a class rotation for the classifier, a
possibly transformed target for the regressor.}

\item{categorical_features}{Passed to \code{\link[=apply_member_pipeline]{apply_member_pipeline()}}.}
}
\value{
List of caches, one per member.
}
\description{
Every member sees a differently preprocessed view of the same training
rows, so every member needs its own cache. Building them costs one
forward pass per member over the training rows; after that a prediction
costs only the test rows, however many batches they arrive in.
}
\details{
Only backends whose network advertises \code{supports_kv_cache} can do this.
}
\keyword{internal}
