% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-utils.R
\name{chunked_evaluate}
\alias{chunked_evaluate}
\title{Evaluate a function over a tensor in chunks, writing back in place}
\usage{
chunked_evaluate(f, x, factor, residual, batch_dims, ...)
}
\arguments{
\item{f}{Function of one tensor, returning a tensor of the same shape.}

\item{x}{Input tensor.}

\item{factor}{Number of chunks, or \code{NULL} to evaluate in one go.}

\item{residual}{Return \code{x + f(x)} rather than \code{f(x)}.}

\item{batch_dims}{How many leading dimensions of \code{x} to fold together
and split along.}

\item{...}{Passed to \code{f}.}
}
\description{
A port of the reference's \code{chunked_evaluate_maybe_inplace}. Every
sublayer of a TabPFN block is independent across some leading set of
dimensions -- a per-token MLP is independent across rows \emph{and} columns,
attention between a row's features is independent across rows -- so the
work can be split along those and done a slice at a time. What that
buys is peak memory, not speed: attention materialises an
\verb{(n, n)} score matrix internally, and doing it in \code{k} pieces makes that
transient \code{k} times smaller.
}
\details{
The result is written back into \code{x} rather than accumulated into a new
tensor, which is the other half of the saving. That only works if
\code{x$flatten()} returns a view instead of a copy, which needs the folded
dimensions to be contiguous -- hence the \code{contiguous()} call, a no-op
when \code{x} already is.

Chunking changes nothing about the arithmetic: each slice sees exactly
the values it would have seen inside the whole.
}
\keyword{internal}
