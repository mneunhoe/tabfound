% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arch-spec.R
\name{tabfound_architecture}
\alias{tabfound_architecture}
\alias{as.data.frame.tabfound_arch}
\title{Describe a loaded model's architecture}
\usage{
tabfound_architecture(object)

\method{as.data.frame}{tabfound_arch}(
  x,
  row.names = NULL,
  optional = FALSE,
  detail = c("overview", "full"),
  ...
)
}
\arguments{
\item{object}{A model from \code{\link[=tabular_classifier]{tabular_classifier()}} or
\code{\link[=tabular_regressor]{tabular_regressor()}} -- fitted or not -- or a \code{\link[=tabfound]{tabfound()}} fit.}

\item{row.names, optional}{Ignored; present for S3 consistency.}

\item{detail}{\code{"overview"} for one row per stage, \code{"full"} to also
include each repeated block's sublayers.}
}
\value{
An object of class \code{tabfound_arch}: a list with \code{backend},
\code{task}, \code{title}, \code{subtitle}, \code{facts}, \code{n_params} and \code{stages}.
}
\description{
Turns a loaded tabular foundation model into a structured description
of its architecture: the ordered stages it runs, the axis each
attention sublayer works over, the tensor shapes between stages, and
the exact parameter count of every stage read off the network itself.
}
\details{
\code{\link[=plot_architecture]{plot_architecture()}} renders this to SVG, PNG or PDF; printing it
gives the same information as text, and \code{as.data.frame()} gives one
row per stage.
}
\examples{
\dontrun{
clf <- tabular_classifier("path/to/tabpfn-v2.5-clf")
arch <- tabfound_architecture(clf)
arch
as.data.frame(arch)
}
}
\seealso{
\code{\link[=plot_architecture]{plot_architecture()}}
}
