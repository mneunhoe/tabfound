% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-text.R
\name{fit_tfidf}
\alias{fit_tfidf}
\title{Fit a tf-idf vectorizer over character n-grams}
\usage{
fit_tfidf(docs)
}
\arguments{
\item{docs}{Character vector, \code{NA} already replaced by \code{""}.}
}
\value{
A fit for \code{\link[=transform_tfidf]{transform_tfidf()}}.
}
\description{
\code{TfidfVectorizer(analyzer = "char_wb", ngram_range = c(3, 4))} with
scikit-learn's defaults: the vocabulary is every n-gram seen, sorted by
code point; \code{smooth_idf} gives \code{idf = log((1 + n) / (1 + df)) + 1}.
}
\details{
Code-point order is what \code{sort(method = "radix")} gives on UTF-8
strings, because UTF-8 byte order \emph{is} code-point order. The default
sort would follow the session's collation instead, and put \code{"B"} next to
\code{"b"} -- a different vocabulary order and so a different matrix.
}
\keyword{internal}
