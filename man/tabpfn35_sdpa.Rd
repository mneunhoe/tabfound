% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn35.R
\name{tabpfn35_sdpa}
\alias{tabpfn35_sdpa}
\title{Scaled dot-product attention on \verb{(B, S, H, D)} tensors}
\usage{
tabpfn35_sdpa(q, k, v, scaling = NULL)
}
\arguments{
\item{scaling}{Optional \code{\link[=tabpfn35_softmax_scaling_mlp]{tabpfn35_softmax_scaling_mlp()}}, applied to the
queries with the key count as its length argument.}
}
\description{
As in v3: queries, keys and values are head-last and are permuted
inside, so the shapes in the callers line up with the reference's
suffix notation one for one. Grouped-query attention falls back to
\code{repeat_interleave}, which is what the reference does in float32 and is
exact either way.
}
\keyword{internal}
