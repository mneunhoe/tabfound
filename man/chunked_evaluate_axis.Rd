% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-utils.R
\name{chunked_evaluate_axis}
\alias{chunked_evaluate_axis}
\title{Evaluate a function over one axis of a tensor in chunks, in place}
\usage{
chunked_evaluate_axis(f, x, factor, axis, residual = TRUE, ...)
}
\arguments{
\item{f}{Function of one tensor, returning a tensor of the same shape.}

\item{x}{Input tensor.}

\item{factor}{Number of chunks, or \code{NULL} to evaluate in one go.}

\item{axis}{1-based axis to split along.}

\item{residual}{Write \code{x + f(x)} rather than \code{f(x)}.}

\item{...}{Passed to \code{f}.}
}
\description{
The same trade as \code{\link[=chunked_evaluate]{chunked_evaluate()}} — peak memory for a little loop
overhead, with the result written back into \code{x} rather than
accumulated — but splitting a named axis instead of a fold of the
leading ones.
}
\details{
That distinction matters where the sublayer's own reshape needs the
leading dimensions kept apart. Mitra's observation attention folds
\verb{(batch, features)} into the attention batch and puts \emph{rows} in the
sequence position, so its key/value cache is indexed by
\code{batch * features}; flattening \verb{(batch, rows)} first, as
\code{\link[=chunked_evaluate]{chunked_evaluate()}} does, would hand a chunk spanning two datasets to
a cache that expects one. Splitting the row axis leaves that fold
intact.

\code{torch_split()} returns views along any axis, so the in-place
\code{add_()} reaches the original storage whichever axis is chosen.
}
\keyword{internal}
