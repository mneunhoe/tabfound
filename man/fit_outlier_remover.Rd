% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{fit_outlier_remover}
\alias{fit_outlier_remover}
\title{Fit the two-stage z-score outlier bounds}
\usage{
fit_outlier_remover(X, threshold = 4)
}
\arguments{
\item{X}{Numeric matrix.}

\item{threshold}{Z-score beyond which a value is an outlier.}
}
\description{
Stage one flags anything beyond \code{threshold} sample standard deviations
and blanks it; stage two recomputes the mean and sd without those
points, so a single wild value cannot inflate the bounds that are
supposed to catch it.
}
\details{
Note the \code{ddof}: the reference uses the \emph{sample} standard deviation
here (\code{ddof = 1}), unlike every other scaler in this file, and drops
to \code{ddof = 0} only for a single row.
}
\keyword{internal}
