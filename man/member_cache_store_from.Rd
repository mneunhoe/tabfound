% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-utils.R
\name{member_cache_store_from}
\alias{member_cache_store_from}
\title{A store pre-filled from a persisted cache}
\usage{
member_cache_store_from(caches)
}
\arguments{
\item{caches}{A named list keyed by member index, as
\code{\link[=tabfound_cache]{tabfound_cache()}} stores it, or \code{NULL}.}
}
\value{
A \code{\link[=member_cache_store]{member_cache_store()}}, or \code{NULL}.
}
\description{
The lazy backends build member caches on first use, which is right
when the cache lives for one \code{predict()} call. A cache restored from
disk is already built, so it is loaded into a store of the same shape
and the builders never run.
}
\keyword{internal}
