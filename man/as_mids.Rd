% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi-interop.R
\name{as_mids}
\alias{as_mids}
\alias{as_mids.tabfound_mi}
\title{Convert to a mice \code{mids} object}
\usage{
as_mids(x, ...)

\method{as_mids}{tabfound_mi}(x, ...)
}
\arguments{
\item{x}{A \code{tabfound_mi} object from \code{\link[=tabfound_impute]{tabfound_impute()}}.}

\item{...}{Unused.}
}
\value{
An object of class \code{mids}.
}
\description{
Repackages imputations as mice's multiply-imputed data set, so the
whole mice toolchain -- \code{with()}, \code{pool()}, \code{complete()},
\code{densityplot()}, \code{stripplot()} -- applies unchanged.
}
\details{
The convergence trace comes with it: \code{\link[=tabfound_impute]{tabfound_impute()}} records the
mean and variance of each variable's imputed cells at every sweep, in
mice's own \verb{variable x iteration x imputation} layout, so \code{plot()} on
the result draws the chains the way it does for a \code{mice()} fit. The
\code{where} matrix travels too.
}
\examples{
\dontrun{
mids <- as_mids(imp)
summary(mice::pool(with(mids, lm(Ozone ~ Wind + Temp))))
mice::densityplot(mids, ~ Ozone)
}
}
