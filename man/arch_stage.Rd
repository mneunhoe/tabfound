% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arch-spec.R
\name{arch_stage}
\alias{arch_stage}
\title{Describe one stage of an architecture}
\usage{
arch_stage(
  id,
  label,
  kind = "embed",
  detail = NULL,
  axis = NULL,
  shape = NULL,
  group = NULL,
  repeats = 1L,
  prefix = character(),
  children = list()
)
}
\arguments{
\item{id}{Short identifier, unique within the architecture.}

\item{label}{Human-facing name, shown inside the block.}

\item{kind}{One of \code{names(ARCH_KINDS)}.}

\item{detail}{Optional second line, e.g. the layer widths.}

\item{axis}{Optional attention axis; one of \code{ARCH_AXES}.}

\item{shape}{Optional symbolic output shape, e.g. \code{"(B, n, F, E)"}.}

\item{group}{Optional name of the phase this stage belongs to.
Consecutive stages sharing a group are bracketed together in the
diagram's left margin.}

\item{repeats}{Integer. How many times this block is stacked.}

\item{prefix}{Character vector of parameter-name prefixes this stage
owns. Used to read exact parameter counts off the network and to
check that every parameter is accounted for.}

\item{children}{Optional list of \code{arch_stage()}s, drawn when
\code{detail = "full"}. Their \code{prefix} entries point at the \emph{first}
member of a repeated stack, so their counts are per block.}
}
\value{
A \code{tabfound_arch_stage}.
}
\description{
Describe one stage of an architecture
}
\keyword{internal}
