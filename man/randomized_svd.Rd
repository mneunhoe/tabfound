% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-text.R
\name{randomized_svd}
\alias{randomized_svd}
\title{Randomized truncated SVD, as scikit-learn computes it}
\usage{
randomized_svd(M, n_components, seed = 0L, n_oversamples = 10L, n_iter = 5L)
}
\arguments{
\item{M}{A matrix (sparse or dense), rows by columns.}

\item{n_components}{Components to keep.}

\item{seed}{The \code{RandomState} seed; \code{TruncatedSVD(random_state = 0)}.}
}
\value{
\code{list(v, d)}: \code{v} is \verb{n_components x ncol(M)} -- scikit-learn's
\code{components_} -- and \code{d} the singular values.
}
\description{
Port of \code{sklearn.utils.extmath._randomized_svd} with the arguments
\code{TruncatedSVD} passes: \code{n_oversamples = 10}, \code{n_iter = 5},
\code{power_iteration_normalizer = "auto"} -- which resolves to LU, since
\code{n_iter > 2} -- \code{transpose = "auto"} and \code{flip_sign = FALSE}, followed by
\code{TruncatedSVD}'s own \code{svd_flip(u_based_decision = FALSE)}.
}
\details{
\code{transpose = "auto"} means that when there are fewer rows than columns the
decomposition runs on \code{t(M)}, so the Gaussian test matrix is drawn with
as many rows as \code{M} has \emph{rows}. Getting that wrong draws a different
shape from the same stream, which is every value different.

The final QR is taken with base R's \code{qr()}. Its sign and pivoting
conventions differ from LAPACK's \code{geqrf}, and it does not matter: any
orthonormal basis of the same range gives the same \code{B} up to an
orthogonal factor, which the SVD absorbs and \code{svd_flip} then removes.
}
\keyword{internal}
