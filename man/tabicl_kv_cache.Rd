% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabicl.R
\name{tabicl_kv_cache}
\alias{tabicl_kv_cache}
\title{Everything the labelled rows contribute to a TabICL prediction}
\usage{
tabicl_kv_cache(col_hidden, icl_kv, n_train, n_features)
}
\arguments{
\item{col_hidden}{Per-block column-stage summaries.}

\item{icl_kv}{Per-block ICL key/value projections.}

\item{n_train}{Number of labelled rows it was built from.}

\item{n_features}{Feature count it was built for; rows of a different
width cannot use it.}
}
\description{
TabICL restricts context to the labelled rows by \strong{slicing}, not
masking: the column stage's inducing points read \code{src[, , 1:train_size, ]}, and every ICL block's keys are \code{norm1(q)[, 1:train_size, ]}. A
labelled row therefore never sees a test row at any stage, so the whole
labelled-row half of the forward pass is a function of the training
data alone and can be computed once. The cache holds the same
quantities the uncached pass computes, arrived at through the same code
path -- it is built by slicing too, which matters because the fused and
split projections in \code{\link[=mha_fused_inproj]{mha_fused_inproj()}} disagree in the last bit and
a stack of blocks amplifies that.
}
\details{
What it is not is bit-identical, and neither is \code{predict_chunk_size}:
dropping the training rows from the batch changes the shapes every
matmul sees, and float32 matmuls are not invariant to that. The
residual is the same size either way -- around \code{1e-7} on a logit, on
the tiny networks the tests use.

Two things are stored:
\itemize{
\item \code{col_hidden} — one \verb{(B, HC, num_inds, E)} summary per column-stage
block. This is the bottleneck the set transformer squeezes the rows
through, so it is small however many training rows there were.
\item \code{icl_kv} — one \code{list(key, value)} per ICL block, over the labelled
rows. This one does grow with the training set.
}

The row stage needs nothing: it attends across a single row's columns,
so it never looked at another row to begin with.
}
\keyword{internal}
