% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn-rope.R
\name{rope}
\alias{rope}
\title{Rotary position embedding with a checkpoint-loaded frequency table}
\usage{
rope(dim, base = 1e+05, interleaved = TRUE, learnable = FALSE, seq_axis = NULL)
}
\arguments{
\item{dim}{Head dimension. The table holds \code{dim / 2} frequencies.}

\item{base}{Frequency base used for the initial (overwritten) values.}

\item{interleaved}{Pairing convention, see above.}

\item{learnable}{When \code{TRUE} the table is a parameter rather than a
buffer, matching a checkpoint that trained it.}

\item{seq_axis}{Which axis carries position. TabFM rotates
\verb{(B, T, N, Dh)} along axis 2; TabICL rotates \verb{(..., H, T, Dh)} along
the second-to-last axis, which is what \code{NULL} selects.}
}
\description{
Rotates pairs of channels of \code{x} by an angle proportional to position
along the sequence axis.
}
\details{
The inverse frequencies are \strong{loaded from the checkpoint}, not
recomputed from \code{base}. TabFM's were computed in bfloat16 at training
time; regenerating them in float32 gives values that differ by ~1e-3,
and the error compounds with sequence length. TabICL's are a learnable
parameter and so are not a closed form at all. \code{base} only seeds the
table before loading.

Two pairing conventions are in use, and they are not interchangeable:
\itemize{
\item \code{interleaved = TRUE} pairs adjacent channels \verb{(0,1), (2,3), ...}
(TabFM, LLaMA).
\item \code{interleaved = FALSE} splits the head dimension in half and pairs
\verb{(i, i + d/2)} (TabICL, whose \code{row_rope_interleaved} config field is
\code{False}).
}
}
\keyword{internal}
