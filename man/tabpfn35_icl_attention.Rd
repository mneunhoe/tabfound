% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn35.R
\name{tabpfn35_icl_attention}
\alias{tabpfn35_icl_attention}
\title{In-context attention: every row attends to the training rows only}
\usage{
tabpfn35_icl_attention(
  embedding_size,
  num_heads,
  head_dim,
  softmax_scaling_layer = NULL,
  num_kv_heads = NULL,
  num_kv_heads_test = NULL
)
}
\description{
The keys and values come from \code{x[1:single_eval_pos]}, so a test row can
see the training rows but neither itself nor the other test rows. No
mask is needed; the restriction is the slice.
}
\details{
Test rows additionally use only the first \code{num_kv_heads_test} key/value
heads, which is what keeps \code{\link[=tabpfn35_kv_cache]{tabpfn35_kv_cache()}} one head wide per
block.

One ordering detail decides whether a cached prediction matches an
uncached one: \code{k_norm} is applied to the freshly projected keys
\emph{before} they are cached, so the cached path must use them as they come
and must not norm them again.
}
\keyword{internal}
