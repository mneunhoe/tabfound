% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn-norm.R
\name{affine_layer_norm}
\alias{affine_layer_norm}
\title{Affine layer normalization with an optional bias}
\usage{
affine_layer_norm(dim, eps = 1e-05, bias = TRUE)
}
\arguments{
\item{dim}{Size of the normalized (last) axis.}

\item{eps}{Added to the variance.}

\item{bias}{Whether to include a learnable bias.}
}
\description{
\code{torch::nn_layer_norm()} offers \code{elementwise_affine}, which is
all-or-nothing: weight \emph{and} bias, or neither. TabICL needs the third
combination — weight but no bias — because its \code{bias_free_ln} flag is
\code{FALSE} in the released classifier and \code{TRUE} in the regressor, so a
hard-coded choice would load one checkpoint and fail the other.
}
\details{
Parameters are named \code{weight} and \code{bias}, matching the checkpoint.
}
\keyword{internal}
