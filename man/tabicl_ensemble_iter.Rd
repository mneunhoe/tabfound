% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-ensemble.R
\name{tabicl_ensemble_iter}
\alias{tabicl_ensemble_iter}
\alias{tabicl_ensemble_transform}
\title{TabICL's ensemble views for a test set, one at a time}
\usage{
tabicl_ensemble_iter(gen, X_test)

tabicl_ensemble_transform(gen, X_test)
}
\arguments{
\item{gen}{Result of \code{\link[=tabicl_ensemble_fit]{tabicl_ensemble_fit()}}.}

\item{X_test}{Numeric matrix of test predictors, already imputed.}
}
\value{
\code{list(n, get)}: the member count, and a function of the member
index.
}
\description{
Each member is a \code{list(X, y)} with \code{X} the train-then-test rows in
that member's feature order and \code{y} its relabelled training targets,
in the same flattened method-then-member order the class permutations
are stored in.
}
\details{
The iterator exists because the eager version holds every member's
full \verb{(n_train + n_test) x p} matrix in one list -- eight for TabICL,
thirty-two for TabFM, hundreds of megabytes on a table of any size --
when the consumer only ever looks at one. What is genuinely shared
between members of a normalisation method, the transformed matrix
itself, is still computed once and held; only the per-member column
slice is deferred. Members are cheapest to visit in order, which is
what every caller does.
}
\keyword{internal}
