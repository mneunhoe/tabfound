% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn35.R
\name{tabpfn35_cell_embedder}
\alias{tabpfn35_cell_embedder}
\title{The v3.5 cell embedder: Fourier over values, linear over metadata}
\usage{
tabpfn35_cell_embedder(
  group_size,
  embed_dim,
  num_freq,
  ecdf_num_frequencies,
  row_chunk_size = 2048L
)
}
\description{
A drop-in replacement for v3's \code{nn_linear(G * 2, E)}, and the one place
the architectures genuinely diverge. The grouped tensor arrives as
three blocks of \code{group_size} channels each -- standard-scaled values,
NaN/Inf indicators, raw ECDF ranks, in that order -- and two \code{E}-wide
embeddings are summed:
}
\details{
\itemize{
\item the values through a \code{\link[=tabpfn35_fourier_group_embedder]{tabpfn35_fourier_group_embedder()}};
\item the whole metadata block through one bias-free linear. The ranks are
lifted to \verb{2K} sin/cos features each on the way in, which is why that
linear is wider than the tensor the caller passes: \code{G * 2} value and
indicator channels plus \verb{G * 2K} rank features.
}

A final LayerNorm puts the sum on the same scale as the target
embedding it is about to be added to.

Rows are chunked because the two Fourier expansions transiently
materialise \verb{(..., G, 2F)} and \verb{(..., G, 2K)} per cell across the whole
row axis at once. Rows are independent here, so chunk-and-concatenate
is exact.
}
\keyword{internal}
