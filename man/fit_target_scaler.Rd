% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{fit_target_scaler}
\alias{fit_target_scaler}
\alias{apply_target_scaler}
\alias{invert_target_scaler}
\title{Fit sklearn's \code{StandardScaler} on a target vector}
\usage{
fit_target_scaler(y)

apply_target_scaler(y, scaler)

invert_target_scaler(y, scaler)
}
\arguments{
\item{y}{Numeric vector.}

\item{scaler}{Result of \code{\link[=fit_target_scaler]{fit_target_scaler()}}.}
}
\value{
A list with \code{mean} and \code{scale}.
}
\description{
Both the TabFM and TabICL regressors are trained and evaluated on
standardized targets: their sklearn wrappers fit a \code{StandardScaler} on
\code{y}, feed the scaled values to the network, and inverse-transform
whatever comes back. Skipping this does not error — it produces
predictions on the wrong scale entirely.
}
\details{
Population standard deviation (\code{ddof = 0}), with a zero variance
mapped to a scale of 1, matching \verb{_handle_zeros_in_scale}.
}
\keyword{internal}
