% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn35.R
\name{tabpfn35_y_encoder}
\alias{tabpfn35_y_encoder}
\title{Both target encoders, side by side}
\usage{
tabpfn35_y_encoder(max_num_classes, embed_dim)
}
\description{
The multitask change in miniature. A v3 checkpoint carries one target
encoder, chosen when the weights were trained; a v3.5 checkpoint
carries both, and the forward pass picks. Both are built whatever the
task, because both are in every checkpoint and the loader is strict.
}
\details{
Named to match the reference's \code{nn.ModuleDict}, so the parameters land
at \verb{<name>.multiclass.embedding.weight} and \verb{<name>.regression.weight}.
}
\keyword{internal}
