% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-text.R
\name{.char_wb_ngrams}
\alias{.char_wb_ngrams}
\title{Character n-grams within word boundaries}
\usage{
.char_wb_ngrams(docs, min_n = 3L, max_n = 4L)
}
\arguments{
\item{docs}{Character vector; \code{NA} should already be \code{""}.}

\item{min_n, max_n}{The n-gram range.}
}
\value{
A list with \code{doc} (the 1-based document of each n-gram) and
\code{gram} (the n-gram), in the reference's emission order.
}
\description{
Port of scikit-learn's \code{CountVectorizer._char_wb_ngrams}. Each word is
padded with one space either side, and every \code{n}-gram of the padded word
is emitted for \code{n} in \code{min_n..max_n}. A word too short for \code{n} is
emitted whole, once, and then contributes nothing longer -- so \code{"a"}
yields \code{" a "} and no 4-gram, where a naive sliding window would yield
nothing at all.
}
\details{
Lowercasing happens first, as \code{lowercase = TRUE} does; accents are left
alone, as \code{strip_accents = None} does.
}
\keyword{internal}
