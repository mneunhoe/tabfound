% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi-interop.R, R/mi.R
\name{complete.tabfound_mi}
\alias{complete.tabfound_mi}
\alias{tabfound_complete}
\title{Extract completed data from a \code{tabfound_mi} object}
\usage{
\method{complete}{tabfound_mi}(data, action = 1L, include = FALSE, ...)

tabfound_complete(x, action = 1L, include = FALSE, ...)
}
\arguments{
\item{data}{A \code{tabfound_mi} object.}

\item{action}{\verb{1..m} for a single completed data set; \code{"all"} for a
list of all of them; \code{"long"} for them stacked with \code{.imp} and \code{.id}
columns; \code{"broad"} for them side by side.}

\item{include}{Include the original, incomplete data as \code{.imp == 0}.
Only meaningful for \code{"long"}, \code{"broad"} and \code{"all"}.}

\item{...}{Unused.}

\item{x}{A \code{tabfound_mi} object.}
}
\value{
A data frame, or a list of them for \code{action = "all"}.
}
\description{
Same contract as \code{mice::complete()}, which this is also registered as
a method for -- so \code{mice::complete(imp, "long")} works on the object
directly, and \code{tabfound_complete()} is the route that needs no mice.
}
\examples{
\dontrun{
tabfound_complete(imp, 1)        # first completed data set
tabfound_complete(imp, "long")   # stacked, with .imp / .id
}
}
