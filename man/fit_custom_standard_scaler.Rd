% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{fit_custom_standard_scaler}
\alias{fit_custom_standard_scaler}
\alias{transform_custom_standard_scaler}
\title{Fit the wrappers' \code{CustomStandardScaler}}
\usage{
fit_custom_standard_scaler(X, clip_min = -100, clip_max = 100, epsilon = 1e-06)

transform_custom_standard_scaler(X, fit)
}
\arguments{
\item{X}{Numeric matrix.}

\item{clip_min, clip_max}{Output clipping bounds.}

\item{epsilon}{Added to every column's standard deviation.}

\item{fit}{Result of \code{\link[=fit_custom_standard_scaler]{fit_custom_standard_scaler()}}.}
}
\description{
Not sklearn's: both wrappers ship their own, and it differs in two
ways that matter. \code{epsilon} is \emph{added} to the standard deviation
rather than triggering a constant-column branch, so a constant column
comes out as \code{(x - mean) / 1e-6} — a huge number, which the clip then
catches. And the output is clipped to \verb{[clip_min, clip_max]}, which is
the only thing standing between a degenerate column and an infinity
downstream.
}
\keyword{internal}
