% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn35.R
\name{tabpfn35_kv_cache}
\alias{tabpfn35_kv_cache}
\title{Everything the training rows contribute to a prediction}
\usage{
tabpfn35_kv_cache(
  kv,
  scaler,
  ecdf_context,
  inducing_hidden,
  train_embeddings,
  y_train,
  n_train
)
}
\arguments{
\item{kv}{List of \code{list(key, value)}, one per ICL block. The keys are
post-\code{k_norm}.}

\item{scaler}{Fitted \code{list(mean, std)}.}

\item{ecdf_context}{The \verb{(3, B, C, K)} bucket context.}

\item{inducing_hidden}{List of inducing summaries, one per
distribution-embedder block.}

\item{train_embeddings}{\verb{(B, n_train, D)} post-norm row embeddings.}

\item{y_train}{\verb{(B, n_train)} targets, for the classifier's decoder.}

\item{n_train}{Number of training rows it was built from.}
}
\description{
TabPFN has no weights to fit, so "training" is conditioning: the
training rows sit in the context of every forward pass. That makes
prediction cost grow with the training set, and grow again for every
batch of test rows, since the context is rebuilt each time.
}
\details{
It does not have to. The training rows reach a test row through exactly
five channels in v3.5, and all five are functions of the training rows
alone:
\itemize{
\item the scaler statistics fitted on them;
\item the ECDF bucket context each column is ranked against -- new in
v3.5, and the reason a cached prediction still sees the same ranks
the uncached one would have produced;
\item the inducing summary of each distribution-embedder block, \code{n_ind}
vectors per column;
\item the key/value projections of each ICL block, one head wide because a
test row attends to one head;
\item the post-norm training row embeddings, which the classifier's
retrieval decoder reads.
}

As in v3, and unlike v2.6's cache, this one changes nothing: nothing is
fitted on test rows, so a cached prediction and an uncached one are the
same computation.
}
\keyword{internal}
