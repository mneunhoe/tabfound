% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{fit_rtdl_quantile_transformer}
\alias{fit_rtdl_quantile_transformer}
\title{Fit the RTDL variant of the quantile transformer}
\usage{
fit_rtdl_quantile_transformer(
  X_train,
  noise = 0.001,
  n_quantiles = 1000L,
  output_distribution = "normal",
  subsample = NULL
)
}
\arguments{
\item{X_train}{Numeric matrix.}

\item{noise}{Relative noise magnitude; \code{0} disables it.}

\item{n_quantiles}{Upper bound on the knot count.}

\item{output_distribution}{\code{"uniform"} or \code{"normal"}.}

\item{subsample}{Passed to \code{\link[=fit_sk_quantile_transformer]{fit_sk_quantile_transformer()}}.}
}
\description{
Two differences from the stock estimator: the knot count is tied to
the row count (\code{n / 30}, floored at 10) rather than fixed, and
Gaussian noise is added to the training data before fitting, which
breaks up ties so the empirical CDF has no flat spots.
}
\details{
\strong{The noise is the one thing here that is not bit-comparable.} The
reference draws it from NumPy's PCG64; this uses R's RNG, so a run
with \code{noise > 0} is reproducible under \code{set.seed()} but is not the
reference's draw. \code{noise = 0} is exact. This only bites the
\code{quantile_rtdl} normalisation method, which is not in either
wrapper's default list.
}
\keyword{internal}
