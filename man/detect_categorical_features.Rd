% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{detect_categorical_features}
\alias{detect_categorical_features}
\title{Decide which predictors are categorical}
\usage{
detect_categorical_features(
  X,
  categorical_features = NULL,
  max_unique = 30L,
  min_unique_numerical = 4L,
  min_samples_inference = 100L
)
}
\arguments{
\item{X}{Numeric matrix.}

\item{categorical_features}{Integer vector of 1-based column indices
the caller declares categorical, or \code{NULL}.}

\item{max_unique}{Cardinality ceiling for a declared column to stay
categorical. The reference's \code{MAX_UNIQUE_FOR_CATEGORICAL_FEATURES}.}

\item{min_unique_numerical}{Below this many distinct values, an
undeclared column is inferred categorical.
\code{MIN_UNIQUE_FOR_NUMERICAL_FEATURES}.}

\item{min_samples_inference}{Inference is only attempted on more than
this many rows. \code{MIN_NUMBER_SAMPLES_FOR_CATEGORICAL_INFERENCE}.}
}
\value{
Sorted integer vector of 1-based categorical column indices.
}
\description{
A port of \code{detect_feature_modalities}, restricted to the numeric
matrices this package takes. The reference's rule is deliberately
asymmetric, and the asymmetry is the whole point:
}
\details{
\itemize{
\item A column the caller \strong{declares} categorical is taken at its word,
unless its cardinality is so high that treating it as categorical
would be useless — more than \code{max_unique} distinct values.
\item A column nobody declared is only \emph{inferred} categorical when it is
extremely low-cardinality (fewer than \code{min_unique_numerical} distinct
values) and there are enough rows to trust the count at all. A
continuous column with a handful of repeats must not be swept up.
}

Distinct values are counted with \code{NA} as a category, matching pandas'
\code{nunique(dropna = FALSE)}.

Constant columns (one distinct value or fewer) are not categorical
unless declared; the reference calls them a modality of their own, and
they are dropped by \code{\link[=remove_constant_features_fit]{remove_constant_features_fit()}} before any of this
matters.
}
\keyword{internal}
