% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabicl.R
\name{tabicl_peak_terms}
\alias{tabicl_peak_terms}
\title{TabICL's peak-memory shapes}
\usage{
tabicl_peak_terms(n_context, n_query, n_features, opts, config)
}
\arguments{
\item{n_context, n_query, n_features}{Dimensions; \code{n_query} should
already be the resident chunk (see \code{\link[=.resident_query]{.resident_query()}}).}

\item{opts}{Resolved predictor arguments.}

\item{config}{The model config.}
}
\description{
Three stages with three different peaks, so the estimate takes the
maximum: the column embedder is wide (\verb{rows x column-groups x embed_dim}) but kept linear in rows by its inducing points, the row
interactor is the same width over a short sequence, and the in-context
stage is quadratic in rows but only \code{embed_dim * row_num_cls} wide.
Summing them would roughly treble the answer.
}
\keyword{internal}
