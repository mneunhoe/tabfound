% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn3.R
\name{tabpfn3_kv_cache}
\alias{tabpfn3_kv_cache}
\title{Everything the training rows contribute to a prediction}
\usage{
tabpfn3_kv_cache(
  kv,
  scaler,
  inducing_hidden,
  train_embeddings,
  y_train,
  n_train
)
}
\arguments{
\item{kv}{List of \code{list(key, value)}, one per ICL block.}

\item{scaler}{Fitted \code{list(mean, std)}.}

\item{inducing_hidden}{List of inducing summaries, one per
distribution-embedder block.}

\item{train_embeddings}{\verb{(B, n_train, D)} post-norm row embeddings.}

\item{y_train}{\verb{(B, n_train)} ordinal targets, for the classifier's
decoder.}

\item{n_train}{Number of training rows it was built from.}
}
\description{
TabPFN has no weights to fit, so "training" is conditioning: the
training rows sit in the context of every forward pass. That makes
prediction cost grow with the training set, and grow again for every
batch of test rows, since the context is rebuilt each time.
}
\details{
In v3 it does not have to. The training rows reach a test row through
exactly four channels, and all four are functions of the training rows
alone:
\itemize{
\item the scaler statistics fitted on them, which are also the values used
to impute NaN and Inf;
\item the inducing summary of each distribution-embedder block, \code{n_ind}
vectors per column;
\item the key/value projections of each ICL block, one head wide because a
test row attends to one head;
\item the post-norm training row embeddings, which the classifier's
retrieval decoder reads.
}

Unlike v2.6's cache, this one changes nothing: v2.6 fits its
constant-column and informative-feature masks over training \emph{and} test
rows in an ordinary pass, so caching them shifts the prediction. v3
fits nothing on test rows, so a cached prediction and an uncached one
are the same computation.
}
\keyword{internal}
