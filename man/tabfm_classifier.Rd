% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabfm.R
\name{tabfm_classifier}
\alias{tabfm_classifier}
\title{Build the TabFM classifier predictor}
\usage{
tabfm_classifier(
  ctx,
  n_estimators = 32L,
  norm_methods = NULL,
  feat_shuffle_method = "random",
  class_shift = TRUE,
  categorical_features = NULL,
  outlier_threshold = 4,
  max_num_features = 500L,
  max_num_rows = NULL,
  softmax_temperature = 0.9,
  average_logits = TRUE,
  random_state = 42L,
  quantile_subsample = NULL,
  n_feature_crosses = 0,
  n_svd_features = 0,
  predict_chunk_size = 512L,
  kv_cache = FALSE,
  row_chunk_size = NULL,
  col_chunk_size = NULL,
  trace_dir = NULL
)
}
\arguments{
\item{ctx}{Loaded-model context from \code{\link[=load_backend_model]{load_backend_model()}}.}

\item{n_estimators}{Number of ensemble members.}

\item{norm_methods}{Normalisation methods to spread across members;
\code{NULL} means \code{c("none", "power")}.}

\item{feat_shuffle_method}{\code{"random"} or \code{"none"}.}

\item{class_shift}{Rotate the class labels per member.}

\item{categorical_features}{Integer vector of 1-based column indices
to treat as categorical. These reach the network as a \code{cat_mask},
which routes their cells through a separate Fourier basis — so
declaring them changes the prediction, not just the bookkeeping.}

\item{outlier_threshold}{Z-score threshold for the outlier clipper.}

\item{max_num_features, max_num_rows}{Per-member subsampling caps, or
\code{NULL} for none.}

\item{softmax_temperature}{Logits are divided by this before the
softmax.}

\item{average_logits}{Average logits and take one softmax, rather than
averaging per-member probabilities.}

\item{random_state}{Seed for the ensemble construction.}

\item{quantile_subsample}{See \code{\link[=fit_sk_quantile_transformer]{fit_sk_quantile_transformer()}}.}

\item{n_feature_crosses, n_svd_features}{Accepted only as \code{0}; see the
error they raise otherwise.}

\item{predict_chunk_size}{Max test rows per forward pass.}

\item{kv_cache}{Condition on the training rows once per \code{predict()}
call and reuse that across every chunk and ensemble member, instead
of re-encoding the whole training set for each. Off by default. With
32 members and a default chunk of 512 rows this is the difference
between one training pass per member and one per member per chunk,
which on a 1.6 B-parameter network is most of the wall clock. It
answers the same question an uncached pass does -- nothing in this
architecture lets a training row see a test row -- to within float32
rounding. See \code{\link[=tabfm_kv_cache]{tabfm_kv_cache()}}.}

\item{trace_dir}{Optional directory for the parity harness.}
}
\description{
Reproduces \code{TabFMClassifier} under its default preset: an ensemble of
\code{n_estimators} views differing in normalisation, feature order and a
cyclic shift of the class labels, averaged as logits and passed
through one temperature-scaled softmax.
}
\keyword{internal}
