% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-memory.R
\name{.icl_family_terms}
\alias{.icl_family_terms}
\title{Terms for the column -> row -> in-context stack}
\usage{
.icl_family_terms(
  n_context,
  n_query,
  n_features,
  embed_dim,
  group_size,
  n_cls,
  col_heads,
  row_heads,
  icl_heads,
  col_inducing,
  icl_blocks,
  cell_tokens = NULL,
  kv_cache = FALSE,
  n_estimators = 1,
  masked_attention = FALSE,
  row_chunk = Inf,
  group_channels = 0,
  col_chunk = Inf
)
}
\arguments{
\item{n_context, n_query, n_features}{Dimensions; \code{n_query} should
already be the resident chunk (see \code{\link[=.resident_query]{.resident_query()}}).}

\item{embed_dim}{Model width.}

\item{group_size}{Features per column token.}

\item{n_cls}{Number of CLS tokens the row stage appends, which is also
the factor between the embedding width and the in-context width.}

\item{col_heads, row_heads, icl_heads}{Head counts per stage.}

\item{col_inducing}{Inducing points in the column stage, which is what
keeps it linear in rows rather than quadratic.}

\item{icl_blocks}{Blocks in the in-context stack, for the cache size.}

\item{cell_tokens}{Column tokens per row, when the backend embeds
whole cells instead of feature groups (TabFM). Defaults to the
number of feature groups.}

\item{kv_cache, n_estimators}{Whether caches are kept, and how many.}
}
\description{
TabICL, TabFM and TabPFN v3 are the same three-stage shape with
different widths: embed each column against the rows, let a row's
columns interact, then attend across rows in context. The stages have
quite different peaks -- the first two are wide and shallow, the third
narrow and quadratic in rows -- so the maximum matters and the sum
would be badly wrong.
}
\keyword{internal}
