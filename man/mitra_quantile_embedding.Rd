% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-mitra.R
\name{mitra_quantile_embedding}
\alias{mitra_quantile_embedding}
\title{Quantile-rank embedding of the feature matrix}
\usage{
mitra_quantile_embedding(x_support, x_query)
}
\arguments{
\item{x_support}{\verb{(B, S, F)}; @param x_query \verb{(B, Q, F)}.}
}
\value{
A list with the transformed \code{support} and \code{query}.
}
\description{
Replaces every value by its bucket index among the 999 quantiles of
the support column, divides by the number of support rows to land in
\verb{[0, 1]}, then centres and scales by the support's own mean and
population variance. Query values are bucketed against the \emph{support's}
quantiles, so nothing about the test set leaks into the mapping.
}
\details{
Carries no parameters — it is a fixed transformation, which is why the
checkpoint has nothing for it.

\strong{Missing values silently delete a column.} \code{torch.quantile} returns
all-\code{NaN} quantiles for a column containing one, \code{searchsorted}
against all-\code{NaN} boundaries returns 0 for every value, the column's
variance is then zero, and the zero-variance guard below flattens it
to zeros. No \code{NaN} reaches the output and no error is raised — the
feature is simply gone. This is the reference's behaviour, reproduced
faithfully — and it is why \code{\link[=mitra_preprocessor_fit]{mitra_preprocessor_fit()}} mean-imputes
before anything reaches here, exactly as AutoGluon's own preprocessor
does. Nothing in the normal flow can hit this path; the note stands
because a caller invoking this function directly still can.
}
\keyword{internal}
