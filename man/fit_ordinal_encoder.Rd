% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{fit_ordinal_encoder}
\alias{fit_ordinal_encoder}
\alias{transform_ordinal_encoder}
\title{Fit sklearn's \code{OrdinalEncoder} on selected columns}
\usage{
fit_ordinal_encoder(X_train, cols)

transform_ordinal_encoder(X, fit, mappings = NULL)
}
\arguments{
\item{X_train}{Numeric matrix to fit on.}

\item{cols}{Integer vector of 1-based columns to encode.}

\item{X}{Matrix to transform.}

\item{fit}{State from \code{\link[=fit_ordinal_encoder]{fit_ordinal_encoder()}}.}

\item{mappings}{Optional list of integer permutations, one per encoded
column, 0-based as the reference draws them. Applied to the codes
(\code{ordinal_shuffled} and friends); \code{NULL} leaves codes in order.}
}
\value{
Fit state for \code{\link[=transform_ordinal_encoder]{transform_ordinal_encoder()}}, including
\code{n_categories} per column (counting the \code{NA} slot), which is the
length the shuffled-code permutations have to match.

Matrix with one column per entry of \code{fit$cols}, in that order.
}
\description{
Categories are the column's sorted distinct values; codes are their
0-based positions. Two details are load-bearing and easy to lose:
}
\details{
\itemize{
\item \code{NA} is a \emph{category} for counting purposes (it sorts last, as NumPy
puts it), so it occupies a code slot — but it is never emitted as a
code. An \code{NA} in, an \code{NA} out.
\item A value not seen during fitting also becomes \code{NA}, which is what
\verb{handle_unknown = "use_encoded_value", unknown_value = nan} does.
The network's own missing-value channel then absorbs it, so an unseen
level at predict time degrades rather than erroring.
}
}
\keyword{internal}
