% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-registry.R
\name{.resolve_backend_config}
\alias{.resolve_backend_config}
\title{Resolve a model's artifacts, config and backend, without loading weights}
\usage{
.resolve_backend_config(model, task, backend = NULL, subfolder = NULL)
}
\arguments{
\item{model}{Local directory, Hub repo id, or registered alias.}

\item{task}{\code{"classification"} or \code{"regression"}.}

\item{backend}{Optional backend name; inferred from the config when
\code{NULL}.}

\item{subfolder}{Optional artifact subfolder, overriding the
backend's default for this task.}
}
\value{
\code{list(paths, config, backend)}.
}
\description{
The first half of \code{\link[=load_backend_model]{load_backend_model()}}, split out so a caller can ask
what a checkpoint \emph{is} -- its backend, and the \code{inference_config} recipe
it carries -- before committing to building it. \code{\link[=tabfound]{tabfound()}} needs that
to decide whether text and date columns are expanded, and it has to
decide before it can say which columns are categorical, which it must
say before the model is constructed.
}
\details{
Reads only \code{config.json}. A catalogue id still resolves to the local
store, asking to download first if needed, exactly as loading would --
so nothing is fetched here that loading would not fetch a moment later.
}
\keyword{internal}
