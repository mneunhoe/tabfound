% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula-interface.R
\name{tabfound}
\alias{tabfound}
\alias{tabfound.formula}
\alias{tabfound.data.frame}
\alias{tabfound.matrix}
\alias{tabfound.default}
\title{Fit a tabular foundation model with a formula or data frame}
\usage{
tabfound(x, ...)

\method{tabfound}{formula}(
  x,
  data,
  model,
  mode = "auto",
  backend = NULL,
  device = "cpu",
  na_action = "auto",
  ...
)

\method{tabfound}{data.frame}(
  x,
  y,
  model,
  mode = "auto",
  backend = NULL,
  device = "cpu",
  na_action = "auto",
  ...
)

\method{tabfound}{matrix}(
  x,
  y,
  model,
  mode = "auto",
  backend = NULL,
  device = "cpu",
  na_action = "auto",
  ...
)

\method{tabfound}{default}(x, ...)
}
\arguments{
\item{x}{A formula, a data frame of predictors, or a matrix.}

\item{...}{Passed to \code{\link[=tabular_classifier]{tabular_classifier()}} / \code{\link[=tabular_regressor]{tabular_regressor()}}.}

\item{data}{When \code{x} is a formula, the data frame to evaluate it in.}

\item{model}{Model artifacts: a local directory, a HuggingFace repo
id, or a registered alias. See \code{\link[=list_backends]{list_backends()}}.}

\item{mode}{\code{"auto"} (default) infers classification from a factor,
character or logical outcome and regression from a numeric one.}

\item{backend}{Optional backend name; inferred from the config when
\code{NULL}.}

\item{device}{One of \code{"cpu"}, \code{"cuda"}, \code{"mps"}.}

\item{na_action}{What to do about missing predictors. \code{"auto"}
(default) passes them through for backends that handle them
(TabPFN, TabFM) and imputes for those that do not (TabICL).
\code{"pass"}, \code{"impute"} and \code{"fail"} force the choice.}

\item{y}{When \code{x} is a data frame or matrix, the outcome vector.}
}
\value{
An object of class \code{tabfound_fit}.
}
\description{
The R-facing interface: takes a formula and a data frame (or \code{x}/\code{y}
directly), works out whether the task is classification or regression
from the outcome's type, encodes predictors to the numeric matrix the
networks expect, and remembers enough to re-apply that encoding to new
data.
}
\examples{
\dontrun{
fit <- tabfound(Species ~ ., data = iris[1:100, ], model = "path/to/model")
predict(fit, iris[101:150, ], type = "prob")
}
}
