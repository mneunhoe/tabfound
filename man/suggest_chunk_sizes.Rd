% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-memory.R
\name{suggest_chunk_sizes}
\alias{suggest_chunk_sizes}
\title{Pick stage-chunk sizes that fit the memory you have}
\usage{
suggest_chunk_sizes(
  model,
  n_context,
  n_query = 1000,
  n_features,
  available = NULL,
  verdict = c("ok", "tight"),
  min_row_chunk = 64L,
  ...
)
}
\arguments{
\item{model}{A \code{tabfound_model}, a catalogued model id, an artifact
directory, or a \code{config.json} read into a list.}

\item{n_context, n_query, n_features}{Dimensions of the intended run.}

\item{available}{Memory to plan against, in bytes. Defaults to what
this machine has free now.}

\item{verdict}{Accept a chunk size that earns at least this verdict:
\code{"ok"} (default) or \code{"tight"}.}

\item{min_row_chunk}{Smallest row chunk worth suggesting.}

\item{...}{Other backend knobs, as \code{\link[=estimate_peak_memory]{estimate_peak_memory()}} takes them.}
}
\value{
A list with \code{row_chunk_size}, \code{col_chunk_size}, \code{peak_bytes},
\code{verdict} and \code{feasible}. When nothing fits, \code{feasible} is \code{FALSE}
and the sizes are the smallest tried — the run is too big for this
machine whatever the chunking, and \code{n_context} is the thing to
change.
}
\description{
The reference implementation halves its chunk size and retries when an
allocation fails. R cannot do that: a libtorch allocation failure kills
the process outright, with no condition to catch and nothing left to
retry from — which is the premise the whole preflight rests on. So the
chunk size has to be chosen \emph{before} the run, from the estimate, and
this is the function that does it.
}
\details{
Only TabPFN v3 has stage chunking. For any other backend this returns
the run's estimate with \code{row_chunk_size = NA}, which is the honest
answer: there is no knob here to turn.
}
\section{What it searches}{

Row chunks from the checkpoint's own default downwards, halving, and
then column chunks the same way if the row axis alone does not get
there. The largest pair that fits wins — smaller chunks cost loop
overhead and buy nothing once the estimate clears.

The order is not arbitrary. A row chunk bounds the forward loop; a
column chunk bounds the summary pre-pass the loop cannot start
without. On a table that is long rather than wide the first is what
binds, and on a wide one the second takes over — measured on TabICL at
12,000 × 300, row chunking alone leaves 36.9 GB and adding a column
chunk of 8 takes it to 29.8 GB.

A \code{NULL} row chunk is never suggested: it is the unchunked pass, and
if that fitted there would be nothing to ask.
}

\examples{
\dontrun{
# 200,000 rows on a laptop with 8 GB free: how small do the chunks
# have to be?
suggest_chunk_sizes("tabpfn-v3-classifier", n_context = 2e5,
                    n_features = 50, available = 8e9)
}
}
\seealso{
\code{\link[=estimate_peak_memory]{estimate_peak_memory()}}, \code{\link[=memory_envelope]{memory_envelope()}}
}
