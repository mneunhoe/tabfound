% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{yeojohnson_llf}
\alias{yeojohnson_llf}
\title{Yeo-Johnson profile log-likelihood}
\usage{
yeojohnson_llf(lambda, x)
}
\arguments{
\item{lambda}{Transform parameter.}

\item{x}{Numeric vector, no missing values.}
}
\description{
\verb{l = -n/2 log(var(y)) + (lambda - 1) sum(sign(x) log1p(|x|))}, where
\code{y} is the transformed data.
}
\details{
All-positive and all-negative columns take a log-space route to the
variance. That is not a micro-optimisation: for a large \verb{|lambda|} the
transformed values overflow float64 outright, and the direct
computation returns \code{inf} where the log-space one returns a finite
number. The optimiser walks through exactly that region.
}
\keyword{internal}
