% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-pipelines.R
\name{load_ensemble_configs_from_dump}
\alias{load_ensemble_configs_from_dump}
\title{Load an ensemble config set from a Python-dumped directory.}
\usage{
load_ensemble_configs_from_dump(dump_dir)
}
\arguments{
\item{dump_dir}{Path to a dumped ensemble directory.}
}
\value{
A list of member specs, each carrying the preprocessing
settings (\code{\link[=apply_member_pipeline]{apply_member_pipeline()}} reads them) plus \code{class_perm}
(0-indexed) and \code{target_transform_lambda}.
}
\description{
The directory must contain \code{ensemble_configs.json} and one
\verb{member_<NN>/shuffle_perm.safetensors} per member, as produced by
\code{inst/parity/tabpfn_reference.py}. Everything the reference draws from
its NumPy generator -- the column shuffle, the class rotation, the
polynomial factor pairs, the target-transform lambda -- is read from
the dump rather than recomputed, because reproducing PCG64 in R is not
worth the days it would take.
}
\keyword{internal}
