% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi.R
\name{with.tabfound_mi}
\alias{with.tabfound_mi}
\title{Run an analysis on every completed data set}
\usage{
\method{with}{tabfound_mi}(data, expr, ...)
}
\arguments{
\item{data}{A \code{tabfound_mi} object.}

\item{expr}{An expression, evaluated with the completed data frame's
columns in scope.}

\item{...}{Unused.}
}
\value{
An object of class \code{mira}.
}
\description{
Evaluates \code{expr} once per imputation and returns the fits in a \code{mira}
object, mice's container for repeated analyses -- so \code{mice::pool()}
takes the result directly, without going through \code{\link[=as_mids]{as_mids()}}.
}
\examples{
\dontrun{
fit <- with(imp, lm(Ozone ~ Wind + Temp))
summary(mice::pool(fit))
}
}
