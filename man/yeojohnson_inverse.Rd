% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{yeojohnson_inverse}
\alias{yeojohnson_inverse}
\title{Inverse Yeo-Johnson, given \code{lambda}.}
\usage{
yeojohnson_inverse(y, lambda)
}
\description{
NaN output is expected, not exceptional: the transform is only
invertible where \code{1 + y * lambda > 0}, and the regressor deliberately
pushes the outermost bar-distribution borders far outside that range.
Those NaNs are the signal that a border is unusable, and
\code{.cancel_nan_borders()} / \code{.repair_borders()} consume them downstream.
The warnings \code{log1p()} would raise are therefore suppressed here.
}
\keyword{internal}
