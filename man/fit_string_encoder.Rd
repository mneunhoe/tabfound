% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-text.R
\name{fit_string_encoder}
\alias{fit_string_encoder}
\title{Fit skrub's \code{StringEncoder} on one column}
\usage{
fit_string_encoder(x, name, n_components = 30L, seed = 0L)
}
\arguments{
\item{x}{Character vector (a factor is read as its labels).}

\item{name}{Column name, for the output names.}

\item{n_components}{Target width.}

\item{seed}{\code{random_state} for the SVD. tabpfn fixes it at 0 so that the
features a column becomes are a property of the data, not of the
estimator's seed.}
}
\value{
A fit for \code{\link[=transform_string_encoder]{transform_string_encoder()}}.
}
\description{
Tf-idf over character n-grams, then a truncated SVD down to
\code{n_components}, then block normalisation. Three branches, as skrub has
them: when both dimensions exceed \code{n_components} the SVD runs; when the
vocabulary is exactly \code{n_components} wide the matrix passes through; and
otherwise its leading \code{n_components} columns are kept. The last two are
why the output can be narrower than asked, and why the width is recorded
at fit.
}
\keyword{internal}
