% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-memory.R
\name{memory_envelope}
\alias{memory_envelope}
\title{How large a table can this model actually take?}
\usage{
memory_envelope(
  model,
  n_features,
  available = NULL,
  n_query = 1000,
  verdict = c("ok", "tight"),
  max_context = 1e+06,
  ...
)
}
\arguments{
\item{model}{A \code{tabfound_model}, a catalogued model id, an artifact
directory, or a \code{config.json} read into a list.}

\item{n_features}{Predictor columns. May be a vector.}

\item{available}{Memory the run will have, in bytes. May be a vector.
Defaults to what this machine has free now.}

\item{n_query}{Rows to predict in the same call.}

\item{verdict}{Largest context that still earns at least this verdict:
\code{"ok"} (default) or \code{"tight"}.}

\item{max_context}{Upper bound for the search.}

\item{...}{Backend knobs, exactly as \code{\link[=estimate_peak_memory]{estimate_peak_memory()}} takes
them: \code{n_estimators}, \code{kv_cache}, \code{predict_chunk_size},
\code{save_peak_memory_factor}.}
}
\value{
A data frame with \code{n_features}, \code{available_bytes},
\code{max_context} and \code{peak_bytes} (what that context is estimated to
cost). \code{max_context} is \code{0} when even one row does not fit.
}
\description{
The inverse of \code{\link[=estimate_peak_memory]{estimate_peak_memory()}}: instead of asking what a
given table costs, ask how big a table fits in a given amount of
memory. Answered by bisection over the estimator, which is monotone in
every dimension, so there is exactly one boundary to find.
}
\details{
Both \code{n_features} and \code{available} may be vectors; the result is one
row per combination, which is what makes it a table. Nothing here
needs weights — a catalogued model resolves against the shipped
architecture dimensions, so this works before any download.
}
\section{What "fits" means}{

\code{verdict = "ok"} returns the largest context the estimator calls
comfortable, which leaves roughly half the usable headroom unspent.
That is the number to plan around. \code{verdict = "tight"} returns the
largest it would not refuse — closer to the true ceiling, and closer
to the point where another process arriving takes the run down with
it. The gap between them is the margin the guard is asking for.
}

\examples{
\dontrun{
# How far does TabICL get on a 16 GB laptop with 10 GB free?
memory_envelope("tabicl-v2-classifier", n_features = c(10, 50, 100),
                available = 10e9)

# The same question across a range of machines.
memory_envelope("tabpfn-v3-classifier", n_features = 50,
                available = c(8, 16, 32, 64) * 1e9)
}
}
\seealso{
\code{\link[=estimate_peak_memory]{estimate_peak_memory()}}
}
