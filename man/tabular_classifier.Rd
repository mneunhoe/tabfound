% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tabular_classifier}
\alias{tabular_classifier}
\title{Load a tabular foundation model for classification}
\usage{
tabular_classifier(
  model,
  backend = NULL,
  device = "cpu",
  categorical_features = NULL,
  ...
)
}
\arguments{
\item{model}{Local directory of model artifacts, a HuggingFace repo
id, or a registered alias (see \code{\link[=list_backends]{list_backends()}}).}

\item{backend}{Optional backend name. Inferred from \code{config.json}
when \code{NULL}.}

\item{device}{One of \code{"cpu"}, \code{"cuda"}, \code{"mps"}.}

\item{categorical_features}{Integer vector of 1-based column indices to
treat as categorical, or \code{NULL} to let the backend infer. Declaring
is much stronger than inferring: inference only catches columns with
fewer than four distinct values, so an ordinal-coded factor with five
levels is invisible to it. \code{\link[=tabfound]{tabfound()}} fills this in from the frame's
factor columns automatically.}

\item{...}{Backend-specific arguments, e.g. \code{ensemble_configs_dir} or
\code{softmax_temperature}.}
}
\value{
An unfitted object of class \code{tabfound_classifier}. Fit it with
\code{\link[=fit]{fit()}}, which returns a fitted copy.
}
\description{
Load a tabular foundation model for classification
}
\examples{
\dontrun{
clf <- tabular_classifier("path/to/tabpfn-v2.5-clf")
clf <- fit(clf, iris[1:100, 1:4], iris[1:100, 5])
predict(clf, iris[101:150, 1:4], type = "prob")
}
}
