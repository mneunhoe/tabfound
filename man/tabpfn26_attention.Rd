% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn26.R
\name{tabpfn26_attention}
\alias{tabpfn26_attention}
\title{TabPFN v2.6 multi-head attention}
\usage{
tabpfn26_attention(embedding_dim, n_heads)
}
\arguments{
\item{embedding_dim}{Model width.}

\item{n_heads}{Number of heads; must divide \code{embedding_dim}.}
}
\description{
Four bias-free linears named exactly as the checkpoint stores them.
One module covers both uses because they differ only in how
\code{forward()} is called:
}
\details{
\itemize{
\item \code{single_eval_pos = NULL} -- attention between the features of one
row. Ordinary self-attention; every feature sees every other.
\item \code{single_eval_pos = n} -- attention between the cells of one column.
Keys and values come from the first \code{n} rows only (thinking rows +
training rows), which is the implicit mask: test rows cannot see
each other or themselves, and so need no explicit mask. Test queries
additionally attend to the \emph{first key/value head only}, broadcast
across all query heads -- multi-query attention, which is what makes
the reference's KV cache one head wide.
}
}
\keyword{internal}
