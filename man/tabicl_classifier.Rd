% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabicl.R
\name{tabicl_classifier}
\alias{tabicl_classifier}
\title{Build the TabICL classifier predictor}
\usage{
tabicl_classifier(
  ctx,
  n_estimators = 8L,
  norm_methods = NULL,
  feat_shuffle_method = "latin",
  class_shuffle_method = "shift",
  outlier_threshold = 4,
  softmax_temperature = 0.9,
  average_logits = TRUE,
  random_state = 42L,
  quantile_subsample = NULL,
  predict_chunk_size = 1024L,
  kv_cache = FALSE,
  row_chunk_size = NULL,
  save_peak_memory_factor = NULL,
  col_chunk_size = NULL
)
}
\arguments{
\item{ctx}{Loaded-model context from \code{\link[=load_backend_model]{load_backend_model()}}.}

\item{n_estimators}{Number of ensemble members.}

\item{norm_methods}{Normalisation methods to spread across members:
any of \code{"none"}, \code{"power"}, \code{"quantile"}, \code{"quantile_rtdl"},
\code{"robust"}. \code{NULL} means \code{c("none", "power")}.}

\item{feat_shuffle_method, class_shuffle_method}{Permutation strategies
for feature order and class labels; see \code{\link[=py_shuffler]{py_shuffler()}}.}

\item{outlier_threshold}{Z-score threshold for the outlier clipper.}

\item{softmax_temperature}{Logits are divided by this before the
softmax.}

\item{average_logits}{Average member logits and then take one softmax
(the reference's default, better calibrated), rather than averaging
each member's probabilities.}

\item{random_state}{Seed for the ensemble construction. Draws come
from a port of CPython's generator, so this reproduces the
reference's members exactly.}

\item{quantile_subsample}{Row cap for the quantile normalisers; see
\code{\link[=fit_sk_quantile_transformer]{fit_sk_quantile_transformer()}}.}

\item{predict_chunk_size}{Max test rows per forward pass. Test rows
only ever attend to training rows, so chunking cannot change the
answer.}

\item{kv_cache}{Condition on the training rows once per \code{predict()}
call and reuse that across every chunk and ensemble member, instead
of re-encoding the whole training set for each. Off by default. It
answers the same question an uncached pass does, rather than an
approximation of it: TabICL restricts context by slicing the
labelled rows out, so a labelled row never sees a test row and
nothing the cache holds could have depended on one. The arithmetic
differs only by the float32 noise a change in batch shape already
produces -- see \code{\link[=tabicl_kv_cache]{tabicl_kv_cache()}}.}
}
\description{
Reproduces \code{TabICLClassifier}: the input is imputed, an ensemble of
\code{n_estimators} differently normalised, differently ordered and
differently labelled views is built, each is run through the network,
and the results are averaged once the class relabelling is undone.
}
\details{
Running a single un-preprocessed forward pass — which is what this
backend used to do — is not a cheaper version of this. The network is
trained on data that has been through this pipeline, so feeding it raw
columns is out of distribution, and one member is one draw from an
ensemble whose whole point is that its members disagree.
}
\keyword{internal}
