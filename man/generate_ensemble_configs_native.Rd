% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-configs.R
\name{generate_ensemble_configs_native}
\alias{generate_ensemble_configs_native}
\title{Generate ensemble configs in pure R (no Python required).}
\usage{
generate_ensemble_configs_native(
  X_train,
  y_train,
  n_estimators = 4L,
  head = c("classifier", "regressor"),
  categorical_features = NULL,
  variant = c("v2.5", "v2.6"),
  random_state = 0L,
  output_dir,
  add_target_transform = TRUE
)
}
\arguments{
\item{X_train}{Numeric matrix \verb{(n, p)}. Used to size the per-member
column permutations and to fit the y target transform.}

\item{y_train}{Integer/factor labels (classifier) or numeric target
(regressor).}

\item{n_estimators}{Integer ensemble size. Typical values: 4, 8.}

\item{head}{\code{"classifier"} or \code{"regressor"}.}

\item{categorical_features}{Integer vector of 1-based column indices to
declare categorical, or \code{NULL} to infer. Must match what the model
will be given at \code{fit()} time -- the configs record a code
permutation per encoded column, and those are sized from this.}

\item{variant}{Which TabPFN generation's member menu to use: \code{"v2.5"}
(the default, for the \code{tabpfn} backend) or \code{"v2.6"} (for
\code{tabpfn26}). Passing the wrong one produces a working but
off-distribution ensemble, so match it to the weights.}

\item{random_state}{Integer seed. Default 0.}

\item{output_dir}{Directory to write configs to (created if missing).}

\item{add_target_transform}{Regressor only, and ignored for \code{"v2.6"}.
If \code{TRUE} (default), every second v2.5 member includes a
Yeo-Johnson target transform with MLE-fitted lambda.}
}
\value{
\code{output_dir}, invisibly.
}
\description{
Produces configs in the same directory layout as
\code{\link[=generate_ensemble_configs]{generate_ensemble_configs()}} but uses R's base RNG for the per-member
draws. Output is NOT bit-identical to Python's \code{TabPFNClassifier} /
\code{TabPFNRegressor} at a given seed, but:
}
\details{
\itemize{
\item ensemble members cycle over the preset list the requested version's
checkpoint actually ships -- \code{squashing_scaler_default} / \code{none} for
TabPFN v2.5's classifier, two flavours of \code{quantile_uni} for v2.6;
\item each member receives a unique shuffle permutation and (classifier)
class permutation derived from \code{random_state};
\item v2.6 regressor members get their polynomial factor pairs drawn the
way the reference draws them;
\item half of the v2.5 regressor members can optionally carry a
Yeo-Johnson target transform fitted via \code{\link[=fit_yeojohnson_lambda]{fit_yeojohnson_lambda()}}.
v2.6 has none: its checkpoint sets the target transform to the
identity.
}

In practice this gives the bulk of the ensembling accuracy benefit
while keeping the inference path fully pure-R.

The member width is measured by running the member's own pipeline
rather than predicted from a formula, so a permutation can never come
out the wrong length as the pipeline grows.
}
\examples{
\dontrun{
d <- tempfile()
generate_ensemble_configs_native(X, y, n_estimators = 4,
                                 head = "classifier", variant = "v2.6",
                                 output_dir = d)
clf <- tabular_classifier("tabpfn-v2.6-classifier", ensemble_configs_dir = d)
}
}
