% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-utils.R
\name{.round_float32}
\alias{.round_float32}
\title{Round doubles to the nearest float32, keeping them as doubles}
\usage{
.round_float32(x)
}
\arguments{
\item{x}{Numeric vector or matrix.}
}
\value{
\code{x}, rounded, with \code{NA} preserved and dimensions kept.
}
\description{
R has no single-precision type, but some reference preprocessing casts
to \code{float32} partway through and carries on from the rounded value --
skrub's \code{DatetimeEncoder} stores seconds since the epoch as float32,
which near 2024 is a resolution of 128 seconds, and its \code{StringEncoder}
hands a float32 tf-idf matrix to the SVD. Skipping the rounding would
give an R port that is \emph{more} precise than the reference and therefore
different from it.
}
\details{
\code{writeBin(size = 4)} performs the IEEE round-to-nearest-even conversion
NumPy's \code{astype(np.float32)} does, subnormals included, so this is exact
rather than an approximation of it. Values beyond float32's range become
\code{Inf}, as they do in NumPy.
}
\keyword{internal}
