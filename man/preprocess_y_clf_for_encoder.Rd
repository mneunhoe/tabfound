% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-inputprep.R
\name{preprocess_y_clf_for_encoder}
\alias{preprocess_y_clf_for_encoder}
\title{Preprocess y for the classifier y encoder}
\usage{
preprocess_y_clf_for_encoder(y, single_eval_pos)
}
\arguments{
\item{y}{Tensor \verb{(B, N, 1)} — test positions should be NaN.}

\item{single_eval_pos}{Integer number of training rows.}
}
\value{
\code{list(main, nan_indicators)}, each \verb{(B, N, 1)}.
}
\description{
Pipeline order (matches Python \code{y_encoder.children()}):
0. NaN handling — signed-sentinel indicator + impute with train mean
of the \emph{raw} y values
\enumerate{
\item MulticlassClassificationTargetEncoder
— \verb{(y > sorted_unique_train_ys).sum(-1)}; runs on the
post-NaN-imputed values, so test rows get an actual rank
based on the imputed mean
}
}
\details{
Order matters: imputing NaN with mean of raw labels (e.g. 1.01 for
iris) and THEN ranking gives a different test-position rank than
ranking first then imputing with mean-of-ranks. Reproducing the
Python order is required for parity.
}
\keyword{internal}
