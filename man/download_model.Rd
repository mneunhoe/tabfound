% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-download.R
\name{download_model}
\alias{download_model}
\title{Download and convert a model's weights}
\usage{
download_model(model, task = NULL, force = FALSE, python = NULL, quiet = FALSE)
}
\arguments{
\item{model}{A catalogue id from \code{\link[=list_models]{list_models()}}, or a family name
(\code{"tabpfn"}, \code{"tabpfn-v2.5"}, \code{"tabpfn-v2.6"}, \code{"tabpfn-v3"},
\code{"tabicl"}, \code{"tabfm-1.0.0"}, \code{"mitra"}) together with \code{task}.
\code{"tabpfn"} stays pinned to v2.5; ask for a later generation by name.
The catalogue carries each family's default checkpoint only; the
specialised v3 variants (\verb{_binary}, \verb{_multiclass}, \verb{_ood},
\verb{_mediumdata}, \verb{_timeseries}) can be converted by hand with
\code{inst/python/tabpfn_convert_ckpt.py} and loaded from a local path.}

\item{task}{\code{"classification"} or \code{"regression"}, when \code{model} names a
family rather than a specific artifact set.}

\item{force}{Re-download and re-convert even if the model is already
in the store.}

\item{python}{Path to the interpreter used for conversion. Defaults to
a search; see Details.}

\item{quiet}{Suppress progress messages.}
}
\value{
The directory holding the artifacts, invisibly.
}
\description{
Fetches a model's artifacts from the Hub into the local store
(\code{\link[=tabfound_home]{tabfound_home()}}) and, for the families that publish PyTorch
pickles, runs the one-time conversion into
\code{model.safetensors} + \code{config.json}. After this the model loads with
no network and no Python.
}
\details{
TabFM and Mitra ship readable artifacts and need no conversion.
TabPFN and TabICL do, and that step needs a Python with \code{torch} and
\code{safetensors} — the checkpoints are nested Python pickles that R's
torch bindings cannot read. Point \code{options(tabfound.python = )} or
\code{TABFOUND_PYTHON} at a suitable interpreter if the default search does
not find one.
}
\examples{
\dontrun{
download_model("tabpfn-v2.5-classifier")
download_model("tabicl", task = "regression")

# Afterwards, loading needs neither network nor Python:
clf <- tabular_classifier("tabpfn-v2.5-classifier")
}
}
\seealso{
\code{\link[=list_models]{list_models()}}, \code{\link[=tabfound_home]{tabfound_home()}}, \code{\link[=remove_model]{remove_model()}}
}
