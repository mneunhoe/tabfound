% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn-norm.R
\name{rms_norm}
\alias{rms_norm}
\title{Root-mean-square layer normalization}
\usage{
rms_norm(dim, eps = 1e-06)
}
\arguments{
\item{dim}{Size of the normalized (last) axis.}

\item{eps}{Added to the mean square before the reciprocal square root.}
}
\description{
\code{x * rsqrt(mean(x^2) + eps) * weight}, with a learnable per-dimension
\code{weight} and no mean subtraction or bias.
}
\details{
The whole computation runs in float32 and is cast back at the end,
even when the input is float32 already — that is what the reference
does, and TabFM applies ~36 of these per forward pass, so the
convention has to be reproduced rather than approximated.
}
\keyword{internal}
