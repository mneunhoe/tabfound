% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-pipelines.R
\name{apply_ensemble_predict_regressor}
\alias{apply_ensemble_predict_regressor}
\title{Run a full ensemble prediction (regressor), averaging bar-dist probs.}
\usage{
apply_ensemble_predict_regressor(
  net,
  col_emb,
  X_train,
  X_test,
  y_train,
  configs,
  borders,
  quantiles = c(0.1, 0.5, 0.9),
  device = "cpu",
  softmax_temperature = 1,
  trace_dir = NULL,
  categorical_features = integer(),
  kv_caches = NULL,
  save_peak_memory_factor = NULL,
  row_chunk_size = NA_integer_,
  col_chunk_size = NA_integer_,
  pipelines = NULL
)
}
\arguments{
\item{net}{Loaded \code{per_feature_transformer} (regressor head).}

\item{col_emb}{Pre-generated column embedding tensor.}

\item{X_train, X_test}{Numeric matrices of raw inputs.}

\item{y_train}{Numeric vector of raw regression targets.}

\item{configs}{List of regressor ensemble-member specs; each must
have \code{target_transform = NULL}.}

\item{borders}{\code{torch_tensor} of length \code{n_bar_bins + 1} -- the
model's \code{criterion$borders}, which live in the \strong{z-normalized
target space} (after Python's \code{(y - mean) / std} step). The
function un-standardizes at the end.}
}
\value{
List with \code{mean}, \code{quantiles}, etc. on the RAW y scale.
}
\description{
Every member's output is re-binned onto the shared border grid before
averaging, including members whose target transform is the identity.
That looks redundant -- the source and destination grids are the same
tensor -- but it is not: a \code{FullSupportBarDistribution} puts half-normal
tails on its outer buckets, so evaluating its CDF at its own borders
re-quantises the tail mass rather than reproducing it. Skipping the
round trip for identity members shifts the predicted mean by ~1e-4 of
its own scale, which is 500x the error that remains once it is done.
}
\keyword{internal}
