% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{apply_fingerprint}
\alias{apply_fingerprint}
\title{Compute per-row fingerprint values for X, matching Python's
\code{AddFingerprintFeaturesStep}.}
\usage{
apply_fingerprint(X, salt_uint64, is_test = FALSE)
}
\arguments{
\item{X}{Numeric matrix with shape \verb{(n, n_features)}, dtype matching
what Python saw at fingerprint time (float32 if post-primary).}

\item{salt_uint64}{The \code{n_cells = n_train_rows * n_features} salt
fitted on training data. Pass the TRAIN salt when transforming
both train and test.}

\item{is_test}{Logical. TRUE for test rows (no collision resolution).}
}
\value{
Numeric vector of length \code{nrow(X)} with values in \verb{[0, 1]}.
}
\description{
Salt = \code{n_train * n_features} where both are taken from the TRAIN
shape (not combined). For \code{is_test = FALSE}, duplicate rows are
re-hashed with incrementing offsets until all hashes are unique.
For \code{is_test = TRUE}, duplicates share the same hash.
}
\keyword{internal}
