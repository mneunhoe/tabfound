% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/py-random.R
\name{py_random}
\alias{py_random}
\title{A CPython-compatible \code{random.Random}}
\usage{
py_random(seed = NULL)
}
\arguments{
\item{seed}{Non-negative integer seed, or \code{NULL} to draw one from R's
own RNG (so \code{set.seed()} still makes a run reproducible). Matches
\code{random.Random(seed)}; CPython takes \code{abs(seed)}, so a negative seed
is folded the same way here.}
}
\value{
An environment with \code{random()}, \code{getrandbits(k)},
\code{randbelow(n)}, \code{shuffle(x)}, \code{sample(population, k)} and
\code{choice(seq)}.
}
\description{
Mutable by design: every draw advances the stream, exactly as the
reference's generator object does. Hold one in a variable and call the
methods on it in the same order the Python code does.
}
\keyword{internal}
