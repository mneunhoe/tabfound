% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn3.R
\name{tabpfn3_describe}
\alias{tabpfn3_describe}
\title{Stage list for the TabPFN v3 diagram}
\usage{
tabpfn3_describe(config, task)
}
\arguments{
\item{config}{The checkpoint's parsed \code{config.json}.}

\item{task}{\code{"classification"} or \code{"regression"}.}
}
\description{
v3 abandons the single stack that attends both ways and splits the
work into three: summarise each column's distribution over rows,
aggregate a row's columns into a fixed-width vector, then run a deep
in-context stack over rows at that wider width. That third stage is
where nearly all the parameters are, and the reason its width is
\verb{4 x embed_dim} is the four CLS tokens the aggregator reads out.
}
\keyword{internal}
