% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn-attention.R
\name{sdpa}
\alias{sdpa}
\title{Scaled dot-product attention}
\usage{
sdpa(
  query,
  key,
  value,
  attn_mask = NULL,
  dropout_p = 0,
  is_causal = FALSE,
  scale = NULL
)
}
\arguments{
\item{query, key, value}{\verb{(B, H, L, D)} tensors.}

\item{attn_mask}{Optional additive or boolean mask.}

\item{dropout_p}{Kept for signature parity; always 0 here (inference).}

\item{is_causal}{Passed through.}

\item{scale}{Optional override for \code{1/sqrt(D)}.}
}
\description{
R torch registers \code{torch_scaled_dot_product_attention} but does not
export it, and every backend here wants it: the fused kernel is what
makes this package's float32 rounding match the reference's, since a
hand-written \verb{softmax(QK'/sqrt(d)) V} accumulates differently.
}
\details{
Reaching into another package's namespace is also a CRAN blocker, and
a hard dependency on an unexported function is a real fragility --
torch is free to rename it. So there is one call site instead of ten,
the lookup is resolved once and cached, and the package still works
when it is gone: \code{.sdpa_fallback()} is the same operation written in
public API, differing only in float rounding (~1e-7 relative on the
package's fixtures, checked in the tests).
}
\section{Options}{

\code{tabfound.sdpa} is \code{"auto"} (default: the fused kernel when torch has
it), \code{"torch"} (require it, error if absent) or \code{"r"} (always the
fallback -- what the agreement test runs).
}

\keyword{internal}
