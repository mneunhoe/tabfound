% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/syn-interop.R
\name{as_synds}
\alias{as_synds}
\alias{as_synds.tabfound_syn}
\title{Convert to a synthpop \code{synds} object}
\usage{
as_synds(x, ...)

\method{as_synds}{tabfound_syn}(x, ...)
}
\arguments{
\item{x}{A \code{tabfound_syn} object from \code{\link[=tabfound_syn]{tabfound_syn()}}.}

\item{...}{Unused.}
}
\value{
An object of class \code{synds}.
}
\description{
Repackages the syntheses as synthpop's synthetic-data object, so the
whole synthpop toolchain applies unchanged: \code{compare()} for
distributional fidelity, \code{utility.gen()} / \code{utility.tab()} for general
utility, \code{lm.synds()} / \code{glm.synds()} for inference under the
synthetic-data variance estimators, and \code{replicated.uniques()},
\code{disclosure()} and \code{sdc()} for the disclosure side.
}
\details{
The skeleton comes from \code{synthpop::syn(data, m = 0)}, which builds the
object without synthesising anything. What is filled in afterwards is
the part that is ours: the syntheses, the visit sequence, the predictor
matrix, \code{k}, and \code{proper}.

Methods are reported as \code{"tabfound"} (or \code{"sample"} for variables drawn
from their marginal, and \code{""} for variables carried through
unsynthesised) — which is what \code{lm.synds()} reads to decide whether a
model is being fitted to partially synthetic data.
}
\examples{
\dontrun{
sds <- as_synds(syn)
synthpop::compare(sds, iris)
summary(synthpop::lm.synds(Sepal.Length ~ Petal.Length + Species, sds))
synthpop::replicated.uniques(sds, iris)$no.uniques
}
}
