% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi-interop.R
\name{mice.impute.tabfound}
\alias{mice.impute.tabfound}
\title{Use a tabular foundation model as a mice imputation method}
\usage{
mice.impute.tabfound(y, ry, x, wy = NULL, models = NULL, draw = "auto", ...)
}
\arguments{
\item{y}{The incomplete target, as mice supplies it.}

\item{ry}{Logical: \code{TRUE} where \code{y} is observed.}

\item{x}{Numeric design matrix of predictors, complete.}

\item{wy}{Logical: rows to impute. Defaults to \code{!ry}.}

\item{models}{A \code{\link[=tabfound_models]{tabfound_models()}} handle. Defaults to
\code{getOption("tabfound.models")}.}

\item{draw}{Passed to the regressor draw; see \code{\link[=tabfound_impute]{tabfound_impute()}}.}

\item{...}{Unused; absorbs the rest of what mice passes down.}
}
\value{
A vector of length \code{sum(wy)}, in \code{y}'s own type.
}
\description{
The mirror image of \code{\link[=tabfound_impute]{tabfound_impute()}}: instead of this package
running the chained-equations loop, mice runs it and calls in here for
each variable. That buys mice's full apparatus -- \code{where}, \code{blocks},
\code{post}, \code{ignore}, convergence plots -- with these models doing the
univariate draws.
}
\details{
\code{mice()} looks the method up by name, so \code{tabfound} must be attached
(\code{library(tabfound)}), and the models have to reach this function
somehow. Either pass \code{models} through \code{mice()}'s \code{...}, or set
\code{options(tabfound.models = tabfound_models(...))} once.

Continuous targets are drawn from the regressor's predictive
distribution, categorical ones from the classifier's predicted
probabilities. Note that mice hands over an already-dummy-coded
numeric design matrix, so factor predictors reach the model one column
per contrast rather than as ordinal codes -- unlike
\code{\link[=tabfound_impute]{tabfound_impute()}}, which keeps them whole.
}
\examples{
\dontrun{
library(tabfound)
options(tabfound.models = tabfound_models(classifier = "...", regressor = "..."))
imp <- mice::mice(airquality, method = "tabfound", m = 5)
summary(mice::pool(with(imp, lm(Ozone ~ Wind + Temp))))
}
}
