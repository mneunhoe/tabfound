% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-registry.R
\name{load_backend_model}
\alias{load_backend_model}
\title{Build and weight-load a model through its backend}
\usage{
load_backend_model(
  model,
  task,
  backend = NULL,
  device = "cpu",
  subfolder = NULL
)
}
\arguments{
\item{model}{Local directory, Hub repo id, or registered alias.}

\item{task}{\code{"classification"} or \code{"regression"}.}

\item{backend}{Optional backend name; inferred from the config when
\code{NULL}.}

\item{device}{Device string.}

\item{subfolder}{Optional artifact subfolder, overriding the
backend's default for this task.}
}
\value{
A list with \code{net}, \code{config}, \code{backend}, \code{device}, \code{task} and
\code{model_ref} (the resolved model string, kept so a saved model can be
rebuilt -- see \code{\link[=tabfound_save]{tabfound_save()}}).
}
\description{
Shared plumbing for \code{\link[=tabular_classifier]{tabular_classifier()}} and \code{\link[=tabular_regressor]{tabular_regressor()}}:
resolve artifacts, read the config, pick a backend, construct the
network, copy the weights in, move to device, set eval mode.
}
\keyword{internal}
