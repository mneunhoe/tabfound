% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-mitra.R
\name{mitra_peak_terms}
\alias{mitra_peak_terms}
\title{Mitra's peak-memory shapes}
\usage{
mitra_peak_terms(n_context, n_query, n_features, opts, config)
}
\arguments{
\item{n_context, n_query, n_features}{Dimensions; \code{n_query} should
already be the resident chunk (see \code{\link[=.resident_query]{.resident_query()}}).}

\item{opts}{Resolved predictor arguments.}

\item{config}{The model config.}
}
\description{
The steepest curve in the package, and the reason the preflight
exists. Every other backend narrows the table down to something small
before attending across rows: TabICL's in-context stage carries one
\code{embed_dim * n_cls} vector per row, TabPFN v2 one vector per feature
\emph{group}. Mitra never narrows. Its state stays \verb{rows x features x dim}
from the first layer to the last, so a 90-column table costs 91 times
what a single column would -- which is what turns 6,426 rows on CPU
from slow into impossible.
}
\details{
Features count one higher than the table's: the target is not a side
input here but column 1 of the same tensor.
}
\keyword{internal}
