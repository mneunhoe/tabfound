% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-ensemble.R
\name{py_shuffler}
\alias{py_shuffler}
\title{Generate permutation patterns for ensemble diversity}
\usage{
py_shuffler(
  n_elements,
  method = "latin",
  n_estimators = 8L,
  max_elements_for_latin = 4000L,
  random_state = NULL
)
}
\arguments{
\item{n_elements}{Number of things to permute.}

\item{method}{One of \code{"none"}, \code{"shift"}, \code{"random"}, \code{"latin"}.}

\item{n_estimators}{How many patterns are wanted. Some methods return
a different number — \code{shift} always returns \code{n_elements}, \code{latin}
likewise — and the caller is expected to cope.}

\item{max_elements_for_latin}{Above this, \code{latin} silently becomes
\code{random}; building the square is quadratic.}

\item{random_state}{Seed for the generator, which is created fresh
here exactly as the reference creates it inside \code{shuffle()}.}
}
\description{
A port of TabICL's \code{Shuffler}. Returns a list of 0-based index
vectors.
}
\details{
The four methods differ in more than flavour. \code{latin} gives a set of
permutations in which every element visits every position exactly once
across the set, which spreads a transformer's position bias evenly;
\code{shift} gives the \code{n} circular rotations; \code{random} draws independent
permutations, exhaustively when there are five or fewer elements.
}
\keyword{internal}
