% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-configs.R
\name{generate_ensemble_configs}
\alias{generate_ensemble_configs}
\title{Generate ensemble configs matching Python's TabPFN}
\usage{
generate_ensemble_configs(
  ckpt,
  X_train,
  y_train,
  n_estimators,
  head = c("classifier", "regressor"),
  random_state = 0L,
  output_dir,
  python_bin,
  no_target_transform = FALSE
)
}
\arguments{
\item{ckpt}{Path to the original \code{.ckpt} file (not the converted
safetensors).}

\item{X_train}{Numeric matrix \verb{(n, p)}.}

\item{y_train}{Integer/factor labels (classifier) or numeric target
(regressor).}

\item{n_estimators}{Integer ensemble size.}

\item{head}{\code{"classifier"} or \code{"regressor"}.}

\item{random_state}{Integer seed. Default 0.}

\item{output_dir}{Directory to write configs to.}

\item{python_bin}{Path to the Python executable. Must have the
\code{tabpfn} package installed (as used for \code{convert_ckpt.py}).}

\item{no_target_transform}{Regressor only: pass \code{TRUE} to disable
target-transform members (still matches Python's output when
\verb{inference_config=\{"REGRESSION_Y_PREPROCESS_TRANSFORMS": (None,)\}}).}
}
\value{
\code{output_dir} invisibly.
}
\description{
Invokes the shipped Python helper (\code{inst/python/generate_configs.py})
which uses the installed \code{tabpfn} package to produce the exact
per-member specs (shuffle permutations, class permutations, target
transform lambdas). The output directory is consumable by
\code{\link[=tabular_classifier]{tabular_classifier()}} / \code{\link[=tabular_regressor]{tabular_regressor()}}'s
\code{ensemble_configs_dir} argument.
}
