% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-frame.R
\name{.text_columns}
\alias{.text_columns}
\title{Which character columns count as text?}
\usage{
.text_columns(data, min_cardinality = 30L, declared = character())
}
\arguments{
\item{data}{A data frame.}

\item{min_cardinality}{Distinct-value count above which a string column
is text.}

\item{declared}{Names of columns declared categorical; never text.}
}
\value{
Names of the text columns.
}
\description{
The reference's rule, from \verb{_text_positions}: a string column -- not a
factor, which is R's \code{category} -- with \emph{more than} \code{min_cardinality}
distinct values counting a missing value as one of them, whose non-missing
values do not all spell numbers, and that the caller has not declared
categorical.
}
\details{
Note the cutoff is 30, the same number as \code{max_unique} in
\code{\link[=detect_categorical_features]{detect_categorical_features()}}, and means the opposite: there, at most 30
levels is what a declared categorical may have; here, more than 30 is
what makes a string column text. They are not one threshold, and should
not be unified.
}
\keyword{internal}
