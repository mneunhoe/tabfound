% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{.np_interp}
\alias{.np_interp}
\title{\code{np.interp}, including its behaviour on repeated breakpoints}
\usage{
.np_interp(x, xp, fp)
}
\arguments{
\item{x}{Values to interpolate at.}

\item{xp}{Ascending breakpoints.}

\item{fp}{Values at the breakpoints.}
}
\description{
R's \code{\link[stats:approxfun]{stats::approx()}} and NumPy's \code{interp} agree everywhere except on
a value that lands exactly on a \emph{repeated} breakpoint, and there they
disagree by a whole interval: NumPy's binary search returns the \strong{last}
matching index, R's bisection may return either.
}
\details{
That looks like a corner case and is not. \code{QuantileTransformer}
interpolates a column against its own quantiles, so every training
value \emph{is} a breakpoint, and any column with ties has repeated ones.
The transformer then interpolates forwards and backwards and averages
the two, precisely to land in the middle of a tie run — which only
works if each direction picks the end of the run NumPy picks. Getting
it wrong shifts tied values by half a rank step.
}
\keyword{internal}
