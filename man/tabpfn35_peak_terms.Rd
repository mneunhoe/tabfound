% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn35.R
\name{tabpfn35_peak_terms}
\alias{tabpfn35_peak_terms}
\title{TabPFN v3.5's peak-memory shapes}
\usage{
tabpfn35_peak_terms(n_context, n_query, n_features, opts, config)
}
\arguments{
\item{n_context, n_query, n_features}{Dimensions; \code{n_query} should
already be the resident chunk (see \code{\link[=.resident_query]{.resident_query()}}).}

\item{opts}{Resolved predictor arguments.}

\item{config}{The model config.}
}
\description{
The same three-stage family as TabICL, TabFM and v3, so it is estimated
the same way, with one term of its own.
}
\details{
The ECDF context is resident for the whole pass -- three floats per
bucket edge per column -- and, when a cache is built, for the whole
\code{predict()} and once per ensemble member. It is bounded by
\code{cell_ecdf_num_buckets} rather than by the row count, which is the
point of bucketing it, so it flattens out on a tall table instead of
growing with it.

Its \emph{transients} -- the sort in the context build, the searchsorted in
the query -- are deliberately not modelled: both are chunked to a fixed
cell budget, so they are a constant independent of the table and belong
in \code{intercept_bytes} rather than in a shape.
}
\keyword{internal}
