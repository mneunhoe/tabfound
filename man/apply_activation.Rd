% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn-mlp.R
\name{apply_activation}
\alias{apply_activation}
\title{Apply a named activation}
\usage{
apply_activation(x, name)
}
\description{
There are two GELUs in circulation and they are not interchangeable:
\code{jax.nn.gelu} defaults to a tanh approximation, while torch's
\code{F.gelu} defaults to the exact erf form. They differ by ~1e-3 at
moderate inputs, which compounds across a stack.
}
\details{
\code{"gelu"} here is the \strong{exact} form, matching torch — that is what a
config field saying \code{"gelu"} means for a PyTorch-native model like
TabICL. Models ported from JAX (TabFM) must ask for \code{"gelu_tanh"}
explicitly.
}
\keyword{internal}
