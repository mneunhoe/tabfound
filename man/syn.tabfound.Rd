% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/syn-interop.R
\name{syn.tabfound}
\alias{syn.tabfound}
\title{Use a tabular foundation model as a synthpop synthesis method}
\usage{
syn.tabfound(
  y,
  x,
  xp,
  smoothing = "",
  proper = FALSE,
  models = NULL,
  draw = NULL,
  draw_mode = NULL,
  donors = NULL,
  clamp = NULL,
  quantile_grid = NULL,
  ...
)
}
\arguments{
\item{y}{The real values of the variable being synthesised.}

\item{x}{The real values of its predictors.}

\item{xp}{The already-synthesised values of those same predictors.}

\item{smoothing}{synthpop's smoothing request; anything other than
\code{""} turns kernel smoothing on for donor-based draw modes.}

\item{proper}{Bootstrap the context before conditioning.}

\item{models, draw, draw_mode, donors, clamp, quantile_grid}{Overrides for
the \code{tabfound.syn} option; see \code{\link[=tabfound_syn]{tabfound_syn()}}.}

\item{...}{Absorbs the rest of what synthpop passes down.}
}
\value{
A list with \code{res} (the synthesised values) and \code{fit}, as
synthpop's method contract requires.
}
\description{
The mirror image of \code{\link[=tabfound_syn]{tabfound_syn()}}: instead of this package running
the synthesis pass, \code{synthpop::syn()} runs it and calls in here for
each variable in the visit sequence. That buys synthpop's full
apparatus — visit sequence and predictor matrix, \code{rules} / \code{rvalues}
constraints, \code{cont.na} and missingness handling, \code{k != n} generation,
and the whole evaluation and inference toolchain on the result.
}
\details{
\code{syn()} looks the method up by name, so \code{tabfound} must be attached
(\code{library(tabfound)}) and the models have to reach this function:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(tabfound.models = tabfound_models(classifier = "...", regressor = "..."))
sds <- synthpop::syn(mydata, method = "tabfound", m = 5, proper = TRUE)
}\if{html}{\out{</div>}}

\code{syn()} refuses \code{...} arguments that are not named for one of its own
methods, so \code{tabfound.draw_mode = "pmm"} is not available. Tuning goes
through an option instead:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(tabfound.syn = list(draw_mode = "pmm", donors = 3))
}\if{html}{\out{</div>}}

Recognised entries are \code{models}, \code{draw}, \code{draw_mode}, \code{donors},
\code{smoothing}, \code{clamp} and \code{quantile_grid}, all documented in
\code{\link[=tabfound_syn]{tabfound_syn()}}.

Two differences from \code{\link[=tabfound_syn]{tabfound_syn()}} are worth knowing. synthpop hands
over its own data frames, so factor predictors reach the model as
ordinal codes taken from the \emph{context's} levels — the query is recoded
against them here, because coding \code{x} and \code{xp} independently would make
the synthetic covariates address different categories than the model
was fitted on. And \code{proper} follows synthpop's default of \code{FALSE},
whereas \code{\link[=tabfound_syn]{tabfound_syn()}} defaults it to \code{TRUE}; see there for why that
matters.
}
\examples{
\dontrun{
library(tabfound)
options(tabfound.models = tabfound_models(classifier = "...", regressor = "..."))
sds <- synthpop::syn(iris, method = "tabfound", m = 5)
synthpop::compare(sds, iris)
}
}
\seealso{
\code{\link[=tabfound_syn]{tabfound_syn()}}, \code{\link[=as_synds]{as_synds()}}
}
