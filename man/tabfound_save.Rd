% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tabfound_save}
\alias{tabfound_save}
\alias{tabfound_load}
\title{Save and reload a tabular foundation model}
\usage{
tabfound_save(object, file)

tabfound_load(file, device = NULL)
}
\arguments{
\item{object}{A \code{tabfound_model}, fitted or not.}

\item{file}{Path to write to.}

\item{device}{Optional device override for the reloaded model.}
}
\value{
\code{file}, invisibly.

For \code{tabfound_load()}, a \code{tabfound_model}.
}
\description{
\code{saveRDS()} does not work on these objects: a torch module survives
the round trip structurally but its tensors come back as dangling
pointers, and the failure only surfaces later, in use. These functions
are the supported path.
}
\details{
What gets written is small — the fitted context (training rows, class
levels, target scaler) plus a \emph{reference} to the model artifacts, not
the weights themselves. Fitting one of these models stores context
rows rather than learning parameters, so the weights on disk are
already the same file the object was built from; duplicating them
would mean writing 6.5 GB per saved TabFM model. \code{\link[=tabfound_load]{tabfound_load()}}
re-resolves them from that reference, so the artifacts (or the
HuggingFace cache) must still be reachable.
}
\examples{
\dontrun{
clf <- fit(tabular_classifier("path/to/model"), X, y)
tabfound_save(clf, "clf.tabfound")
clf2 <- tabfound_load("clf.tabfound")
}
}
