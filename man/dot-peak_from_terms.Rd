% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-memory.R
\name{.peak_from_terms}
\alias{.peak_from_terms}
\title{Turn a backend's shapes into bytes, given fitted constants}
\usage{
.peak_from_terms(stages, weights, persistent, co, spmf = 1, n_estimators = 1)
}
\arguments{
\item{stages}{List of \code{list(name, act, att)}, in tensor elements.}

\item{weights, persistent}{Bytes.}

\item{co}{Coefficients from \code{\link[=.memory_coefs]{.memory_coefs()}}.}

\item{spmf}{\code{save_peak_memory_factor}, or 1.}

\item{n_estimators}{Ensemble members the call will run.}
}
\description{
The one place the scaling model is actually evaluated. Split out
because \code{inst/memory/calibrate.R} fits against exactly this expression
and \code{tests/testthat/test-memory-calibration.R} replays stored
measurements through it: if the assembly here and the assembly the
constants were fitted to ever drift apart, the constants stop meaning
anything, and nothing would say so.
}
\details{
Only one stage is live at a time, so the transient is the largest, not
the sum. \code{save_peak_memory_factor} divides \code{act_copies - 1} rather than
\code{act_copies}: it chunks the temporaries but not the state tensor they
are made from.
}
\section{Ensembles}{

Members run one after another, so the \emph{live set} is one member's. The
resident high-water mark is not: measured on TabICL, going from one
member to sixteen at fixed dimensions took the peak from 3.1 GB to
8.7 GB, in steps that look like an allocator taking a bigger arena and
not giving it back. TabFM behaves the same way. So the transient is
multiplied by \code{1 + ensemble_log2_factor * log2(n_estimators)} --
growth, but nothing like the \verb{n_estimators x} a naive reading would
give, and flat for the single-member default.

This is the term the hand-off that commissioned this work explicitly
said not to include. It was right about the live set and wrong about
resident memory, which is what the OS kills on; the measurement is in
\verb{inst/memory/measurements/ensemble-*.json}.
}

\keyword{internal}
