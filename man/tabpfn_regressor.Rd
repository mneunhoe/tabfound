% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn.R
\name{tabpfn_regressor}
\alias{tabpfn_regressor}
\title{Build the TabPFN regressor predictor}
\usage{
tabpfn_regressor(
  ctx,
  ensemble_configs_dir = NULL,
  predict_chunk_size = 1024L,
  softmax_temperature = 0.9,
  trace_dir = NULL,
  categorical_features = NULL,
  kv_cache = FALSE,
  save_peak_memory_factor = NULL,
  row_chunk_size = NA_integer_,
  col_chunk_size = NA_integer_
)
}
\arguments{
\item{ctx}{Loaded-model context from \code{\link[=load_backend_model]{load_backend_model()}}.}

\item{ensemble_configs_dir}{Optional path to a directory produced by
\verb{inst/python/tabpfn_dump_ensemble.py --head classifier}. When
supplied, \verb{$predict_proba()} averages over all members; otherwise a
single forward pass is used.}

\item{predict_chunk_size}{Integer. Max test rows per forward pass.}

\item{softmax_temperature}{Numeric. Decoder logits are divided by this
before the softmax. Defaults to \code{0.9}, matching the reference
\code{TabPFNClassifier} / \code{TabPFNRegressor} default; set to \code{1} for the
untempered distribution.}

\item{trace_dir}{Optional directory. When set, each ensemble member's
post-preprocessing inputs and raw logits are written there in the
same layout the Python parity harness produces, so the two can be
diffed file-for-file. See \verb{inst/parity/}.}

\item{categorical_features}{Integer vector of 1-based column indices to
treat as categorical, or \code{NULL} to infer. Only the ensemble path uses
it: a single forward pass hands the matrix to the network unchanged.
See \code{\link[=detect_categorical_features]{detect_categorical_features()}} for what inference will and will
not pick up on its own.}

\item{kv_cache}{Condition on the training rows once per \code{predict()}
call and reuse that across every prediction chunk and ensemble
member, instead of rebuilding the whole context per chunk. Cuts the
cost of predicting many rows from \code{n_members * n_chunks} passes over
the training set to \code{n_members}. Off by default. On \code{tabpfn} and
\code{tabpfn3} it is exactly equivalent to an uncached pass, because every
statistic those architectures fit comes from the training rows alone.
On \code{tabpfn26} it is not: that generation fits its constant-column and
informative-feature masks over train and test together, so caching
them can change the prediction -- see \code{\link[=tabpfn26_kv_cache]{tabpfn26_kv_cache()}}.
Supported by the \code{tabpfn} (v2.5), \code{tabpfn26} and \code{tabpfn3} backends.}

\item{save_peak_memory_factor}{Integer, or \code{NULL} (default) to disable.
Splits each sublayer's work into this many chunks, shrinking the
attention score matrix each one materialises. Bit-identical output,
lower peak memory, slightly more overhead. Supported by the
\code{tabpfn26} and \code{tabpfn3} backends.}

\item{row_chunk_size, col_chunk_size}{Stage-0-2 chunking, \code{tabpfn3}
only. The first drives the cell embedding, distribution embedder and
column aggregator a chunk of rows at a time, so the
\verb{(rows, columns, embedding)} tensor is never resident whole; the
second bounds the column-wise pre-pass that builds the distribution
embedder's inducing summaries. Left alone, both take the checkpoint's
own values -- 2048 and 4, which is what the Python reference does by
default on this architecture. \code{NULL} runs every row in one pass,
which is what this package did before the chunking landed.

Unlike \code{save_peak_memory_factor} this is not bit-identical: it
changes the batch shapes the attention kernel sees, and on the
package's large fixture it moves the logits by 1.4e-5 of their own
scale -- less than the reference's own chunked pass moves them.}
}
\description{
Build the TabPFN regressor predictor
}
\keyword{internal}
