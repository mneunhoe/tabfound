% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn3.R
\name{tabpfn3_peak_terms}
\alias{tabpfn3_peak_terms}
\title{TabPFN v3's peak-memory shapes}
\usage{
tabpfn3_peak_terms(n_context, n_query, n_features, opts, config)
}
\arguments{
\item{n_context, n_query, n_features}{Dimensions; \code{n_query} should
already be the resident chunk (see \code{\link[=.resident_query]{.resident_query()}}).}

\item{opts}{Resolved predictor arguments.}

\item{config}{The model config.}
}
\description{
v3 left v2's per-feature transformer behind for the same three-stage
shape TabICL and TabFM use -- embed each feature's distribution
against inducing points, aggregate a row's features, then attend
across rows in context -- so it is estimated the same way, and it is
much the cheapest of the six at fold-sized tables.
}
\details{
One caveat belongs in the estimate rather than in a footnote: the
reference's \verb{_stages_0_to_2} row/column chunking is deliberately not
ported (see the README's known gaps), so this port's peak is \emph{higher}
than the Python reference's on large tables. The preflight is the
compensating control, and says so in its suggestions.
}
\keyword{internal}
