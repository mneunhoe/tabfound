% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-ensemble.R
\name{tabfm_ensemble_fit}
\alias{tabfm_ensemble_fit}
\title{Build TabFM's ensemble of dataset views}
\usage{
tabfm_ensemble_fit(
  X,
  y,
  task = "classification",
  n_estimators = 32L,
  norm_methods = c("none", "power"),
  feat_shuffle_method = "random",
  class_shift = TRUE,
  cat_features = NULL,
  outlier_threshold = 4,
  max_num_features = 500L,
  max_num_rows = NULL,
  random_state = 42L,
  quantile_subsample = NULL
)
}
\arguments{
\item{X}{Numeric matrix of training predictors.}

\item{y}{Targets: 0-based class ids, or numerics for regression.}

\item{task}{\code{"classification"} or \code{"regression"}.}

\item{n_estimators}{Number of members.}

\item{norm_methods}{Character vector of normalisation methods.}

\item{feat_shuffle_method}{\code{"random"} or \code{"none"}.}

\item{class_shift}{Apply the class rotation (classification only).}

\item{cat_features}{1-based indices of categorical columns, or \code{NULL}.}

\item{outlier_threshold}{Passed to \code{\link[=fit_preprocessing_pipeline]{fit_preprocessing_pipeline()}}.}

\item{max_num_features}{Cap on columns sampled per member.}

\item{max_num_rows}{Cap on training rows sampled per member.}

\item{random_state}{Integer seed.}

\item{quantile_subsample}{See \code{\link[=fit_sk_quantile_transformer]{fit_sk_quantile_transformer()}}.}
}
\description{
Same idea as TabICL's, different construction throughout.
}
\details{
Feature orders come from plain random samples rather than a Latin
square. Class relabelling is a \emph{rotation} by an offset drawn without
replacement from \code{0:(n_classes - 1)} and then cycled, not an arbitrary
permutation. Normalisation methods are handed out round-robin over the
shuffled member list, and grouping is by the caller's method order
rather than by set iteration.

Categorical columns are tracked through the constant-column filter so
the network can be told which they are — see the \code{cat_mask} argument
of \code{\link[=tabfm_model]{tabfm_model()}}.
}
\keyword{internal}
