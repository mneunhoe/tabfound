% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{apply_class_permutation_inverse}
\alias{apply_class_permutation_inverse}
\title{Invert a class permutation applied to predicted probabilities.}
\usage{
apply_class_permutation_inverse(probs, perm)
}
\arguments{
\item{probs}{Matrix \verb{(n_test, n_classes)} -- outputs from a single
ensemble member, where column p was the permuted class p-1.}

\item{perm}{\code{class_perm + 1L} (1-indexed). \code{perm[k]} is the column
of \code{probs} that corresponds to original class \code{k-1}.}
}
\description{
If \code{class_perm[k] = p} mapped original class \code{k} to permuted label \code{p}
for training, then the model's column \code{p} equals \code{P(orig = k)}. So
\code{probs_orig[, k] = probs[, class_perm[k]]}
directly. No \code{order()} / inverse computation needed.
}
\keyword{internal}
