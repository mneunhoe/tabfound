% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabfm.R
\name{tabfm_kv_cache}
\alias{tabfm_kv_cache}
\title{Everything the training rows contribute to a TabFM prediction}
\usage{
tabfm_kv_cache(col_hidden, icl_kv, n_train, n_features, has_cat_mask)
}
\arguments{
\item{col_hidden}{Length-2 list, one entry per column stage.}

\item{icl_kv}{Per-block ICL key/value projections.}

\item{n_train, n_features}{What it was built from.}

\item{has_cat_mask}{Whether a categorical mask was in force. Cells are
embedded against a different Fourier basis with one, so a cache
cannot cross that boundary.}
}
\description{
TabFM restricts context to the training rows by \strong{masking}: the two
column stages mask the inducing attention, and the ICL stage masks its
keys. A masked position contributes exactly zero, so a training row's
path through the network never picks up anything from a test row --
which is what makes the training half computable once and reusable.
}
\details{
Two kinds of thing are stored:
\itemize{
\item \code{col_hidden} — for each of the two column stages, one
\verb{(B * HC, num_inds, E)} inducing summary per block. Small: the set
transformer's whole point is that this bottleneck does not grow with
the number of rows.
\item \code{icl_kv} — one \code{list(key, value)} per ICL block, over the training
rows. This one does grow with the training set.
}

Both row stages need nothing cached: each row is its own sequence over
its own columns, so they never looked across rows at all.

Reusing the cache asks the same question a full pass asks, and gets an
answer that agrees to within float32 rounding rather than exactly. The
reason is the masking: a cached attention reduces over \code{n_train}
positions where the uncached one reduces over \code{T} positions of which
\code{T - n_train} contribute exactly zero. The sums are equal in exact
arithmetic and grouped differently in float32. Measured on the test
networks the gap is around \code{1e-6} of the logit scale -- the same order
\code{predict_chunk_size} already moves things by.
}
\keyword{internal}
