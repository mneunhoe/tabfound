% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi.R
\name{tabfound_models}
\alias{tabfound_models}
\title{Model handles for multiple imputation}
\usage{
tabfound_models(
  model = NULL,
  classifier = NULL,
  regressor = NULL,
  device = "cpu",
  ...
)
}
\arguments{
\item{model}{Artifacts to use for both tasks: a local directory, a
HuggingFace repo id, or a registered alias (see \code{\link[=list_backends]{list_backends()}}).
For families that ship one checkpoint per task -- TabPFN does --
give \code{classifier} and \code{regressor} instead.}

\item{classifier, regressor}{Task-specific overrides. Either a model
reference as above, or an already-constructed (and possibly already
configured) \code{\link[=tabular_classifier]{tabular_classifier()}} / \code{\link[=tabular_regressor]{tabular_regressor()}} object.}

\item{device}{One of \code{"cpu"}, \code{"cuda"}, \code{"mps"}.}

\item{...}{Passed to \code{\link[=tabular_classifier]{tabular_classifier()}} / \code{\link[=tabular_regressor]{tabular_regressor()}},
e.g. \code{softmax_temperature}.}
}
\value{
An object of class \code{tabfound_models}.
}
\description{
Chained equations need a classifier for the categorical variables and
a regressor for the continuous ones. Most families ship those as two
separate checkpoints, and loading either one costs anywhere from a
second to a minute, so \code{\link[=tabfound_impute]{tabfound_impute()}} takes a \emph{handle} that loads
each at most once, on first use, and reuses it for every variable,
sweep and imputation.
}
\details{
A model that is never needed is never loaded: a data frame with no
incomplete factors will not touch the classifier.
}
\examples{
\dontrun{
mods <- tabfound_models(classifier = "path/to/tabpfn-v2.5-clf",
                        regressor  = "path/to/tabpfn-v2.5-reg")
imp <- tabfound_impute(airquality, m = 5, models = mods)
}
}
