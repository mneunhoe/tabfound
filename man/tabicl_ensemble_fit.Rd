% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-ensemble.R
\name{tabicl_ensemble_fit}
\alias{tabicl_ensemble_fit}
\title{Build TabICL's ensemble of dataset views}
\usage{
tabicl_ensemble_fit(
  X,
  y,
  classification,
  n_estimators = 8L,
  norm_methods = c("none", "power"),
  feat_shuffle_method = "latin",
  class_shuffle_method = "shift",
  outlier_threshold = 4,
  random_state = 42L,
  quantile_subsample = NULL
)
}
\arguments{
\item{X}{Numeric matrix of training predictors, already imputed.}

\item{y}{Integer vector of 0-based class ids (classification) or
numeric targets (regression).}

\item{classification}{Logical.}

\item{n_estimators}{Number of ensemble members requested.}

\item{norm_methods}{Character vector of normalisation methods.}

\item{feat_shuffle_method, class_shuffle_method}{Passed to \code{\link[=py_shuffler]{py_shuffler()}}.}

\item{outlier_threshold}{Passed to \code{\link[=fit_preprocessing_pipeline]{fit_preprocessing_pipeline()}}.}

\item{random_state}{Integer seed.}

\item{quantile_subsample}{See \code{\link[=fit_sk_quantile_transformer]{fit_sk_quantile_transformer()}}.}
}
\description{
Drops single-valued columns, builds \code{n_estimators} (feature
permutation, class permutation, normalisation) triples, and fits one
preprocessing pipeline per normalisation method in use.
}
\details{
Members are grouped by normalisation method, because the pipeline is
the expensive part and every member sharing a method shares its fit.
That grouping is also the order predictions come back in, so the class
permutations are stored the same way and stay aligned with them.

Fewer than \code{n_estimators} members can come out. With two features and
two classes there are only so many distinct views, and the reference
takes however many the product yields rather than repeating itself.
}
\keyword{internal}
