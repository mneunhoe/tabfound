% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn-ssmax.R
\name{ssmax_qa_mlp}
\alias{ssmax_qa_mlp}
\title{Query-aware scalable softmax (elementwise)}
\usage{
ssmax_qa_mlp(n_heads, head_dim, n_hidden = 64L)
}
\arguments{
\item{n_heads}{Number of attention heads.}

\item{head_dim}{Per-head dimension.}

\item{n_hidden}{Hidden width of both MLPs.}
}
\description{
Scales queries by a factor that depends on the source length \strong{and}
on the query itself:
}
\details{
scale = base_mlp(log n) * (1 + tanh(query_mlp(q)))

\code{base_mlp} maps the scalar \verb{log n} to a per-head, per-dimension base
scale; \code{query_mlp} maps each query vector to a modulation in
\verb{(0, 2)}. Both are two-layer MLPs with an exact-erf GELU between them
— TabICL uses \code{nn.GELU()}, not the tanh approximation TabFM inherits
from JAX.

Held under \verb{attn.ssmax_layer.*} in the checkpoint, with the linears at
\code{base_mlp.0} / \code{base_mlp.2} (index 1 is the parameterless activation).
}
\keyword{internal}
