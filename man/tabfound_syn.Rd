% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/syn.R
\name{tabfound_syn}
\alias{tabfound_syn}
\title{Synthetic data with a tabular foundation model}
\usage{
tabfound_syn(
  data,
  m = 1L,
  models = NULL,
  k = NULL,
  visit_sequence = NULL,
  predictors = NULL,
  method = NULL,
  draw = c("auto", "sample", "grid", "quantile", "residual"),
  draw_mode = c("predictive", "pmm", "rank"),
  donors = 5L,
  smoothing = FALSE,
  clamp = TRUE,
  proper = TRUE,
  cont_na = NULL,
  quantile_grid = 199L,
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame (or matrix) to synthesise from.}

\item{m}{Number of synthetic data sets.}

\item{models}{A \code{\link[=tabfound_models]{tabfound_models()}} handle. A model reference or a
single fitted model object is accepted too and wrapped for you; when
\code{NULL}, \code{...} is passed to \code{\link[=tabfound_models]{tabfound_models()}}, so
\code{tabfound_syn(data, model = "...")} works.}

\item{k}{Rows per synthetic data set. Defaults to \code{nrow(data)}.
Note that \code{k != nrow(data)} changes which variance estimator applies
downstream.}

\item{visit_sequence}{Column names (or indices) in the order they are
synthesised. Defaults to the column order. With no iteration to wash
it out, the order matters — quantify its effect rather than assuming
it away.}

\item{predictors}{Optional 0/1 matrix, rows and columns both named by
the columns of \code{data}, in mice's \code{predictorMatrix} layout:
\code{predictors[v, w] == 1} means \code{w} is used to synthesise \code{v}. Entries
pointing at variables not yet generated are dropped with a warning.
Defaults to everything already available.}

\item{method}{Optional per-variable override. A single string, or a
named vector indexed by column name. One of \code{"regression"},
\code{"classification"}, \code{"sample"} (a bootstrap of the observed values,
no model) or \code{""} (not synthesised — the real column is carried
through, which needs \code{k == nrow(data)}).}

\item{draw}{How to turn a regressor's prediction into a draw; see
\code{\link[=tabfound_impute]{tabfound_impute()}}. Never a point prediction: a posterior mean would
make every synthetic record a fitted value and collapse the
within-conditional variance.}

\item{draw_mode}{Continuous sampling mode: \code{"predictive"}, \code{"pmm"} or
\code{"rank"}. See Details.}

\item{donors}{Donor pool size for \code{draw_mode = "pmm"}.}

\item{smoothing}{Apply kernel smoothing to donor draws, as synthpop's
\code{smoothing = "density"} does. Ignored for \code{"predictive"}, which is
already smooth.}

\item{clamp}{Clamp \code{"predictive"} draws to the observed range.}

\item{proper}{Bootstrap the context rows once per synthesis. See
Details; \code{TRUE} by default, unlike \code{synthpop::syn()}.}

\item{cont_na}{Named list of extra values per column to treat as point
masses rather than as draws from the continuous part, e.g.
\code{list(income = 0)}. \code{NA} is always treated this way.}

\item{quantile_grid}{Number of quantile levels for \code{draw = "quantile"}.}

\item{seed}{Optional integer seed.}

\item{verbose}{Show a progress bar.}

\item{...}{Passed to \code{\link[=tabfound_models]{tabfound_models()}} when \code{models} is \code{NULL}.}
}
\value{
An object of class \code{tabfound_syn}:
\itemize{
\item \code{syn} — list of \code{m} synthetic data frames
\item \code{data} — the input, unchanged
\item \code{method}, \code{m}, \code{k}, \code{n}, \code{visit_sequence}, \code{predictors},
\code{draw_mode}, \code{proper}, \code{seed}, \code{call}
}
}
\description{
Sequential (fully conditional) synthesis, the way \code{synthpop::syn()}
does it, with these models supplying the conditionals. Each variable in
the visit sequence is modelled on the \strong{real} data given the variables
before it, then drawn at the \strong{synthetic} values of those variables
generated earlier in the same pass. The first variable is a bootstrap
of its real marginal. One pass, no iteration.
}
\details{
This is the generative sibling of \code{\link[=tabfound_impute]{tabfound_impute()}}: same models,
same heads, a different contract. Imputation fills the missing cells
conditioning on everything else and sweeps to a stationary
distribution; synthesis replaces every cell conditioning only on what
has already been generated, in one ordered pass.
}
\section{Sampling modes}{
A CART leaf is a set of observed values, so \code{synthpop::syn.cart()}
cannot produce an unobserved value, leave the observed range, or break
integrality. A bar distribution is a continuum and can do all three.
\code{draw_mode} is that tradeoff, and it belongs in the write-up rather
than in a default nobody looks at:
\itemize{
\item \code{"predictive"} — return the draw, restoring integrality and (with
\code{clamp}) the observed range. Smooth and genuinely novel values, low
exact-replication risk; point masses get smeared.
\item \code{"pmm"} — draw, then return a real donor from the \code{donors} observed
values nearest the draw. CART's support semantics exactly, at the
highest replication-disclosure risk.
\item \code{"rank"} — draw for every row, then map the draws onto the observed
order statistics by rank. Reproduces the marginal exactly, which also
means that part of the fidelity is by construction rather than by
merit. "Exactly" means the \emph{context's} marginal, so under
\code{proper = TRUE} it is the bootstrapped one.
}

\code{clamp} interacts with all of this: it holds \code{"predictive"} draws
inside the observed range by flooring and capping them, which piles the
excess mass onto the observed minimum and maximum. That is a boundary
artefact, not a modelled point mass — if a variable has a genuine spike
at its minimum, say it with \code{cont_na} rather than letting \code{clamp}
manufacture one.

Categorical variables need no such choice: a softmax over levels and
one categorical draw per row already \emph{is} the analogue of drawing from
a leaf's class proportions.
}

\section{Properness}{
These models draw each query row independently given a fixed context,
so between-synthesis variance carries predictive noise and no parameter
uncertainty — the same defect that makes an imputer improper under
Rubin's rules. The synthetic-data variance estimators
(\code{synthpop::lm.synds()} and friends) assume the mechanism propagates
it. \code{proper = TRUE} bootstraps the context rows before conditioning,
once per synthesis, which is exactly synthpop's definition of
properness, and is the default here. It costs one fit per synthesis
instead of one fit shared by all of them.
}

\section{Missing and special values}{
\code{NA} is a value to reproduce, not a hole to fill. In a categorical
variable it becomes a level the classifier draws like any other. In a
continuous variable it is modelled in two parts — a categorical model
for \emph{which} special value (or "a real number"), then the regressor for
the real numbers — because a bar distribution cannot put mass on a
point. \code{cont_na} extends that to any other spike, such as a zero-
inflated income variable: \code{cont_na = list(income = 0)}.
}

\section{Scope}{
The context is the real data at generation time. This is a fully
conditional synthesiser, not a disclosure-control method, and it
supports no differential-privacy claim. Assess replication and
attribute disclosure before releasing anything —
\code{synthpop::replicated.uniques()} and \code{synthpop::disclosure()} take the
result via \code{\link[=as_synds]{as_synds()}}.
}

\examples{
\dontrun{
mods <- tabfound_models(classifier = "path/to/tabpfn-v2.5-clf",
                        regressor  = "path/to/tabpfn-v2.5-reg")
sds <- tabfound_syn(iris, m = 5, models = mods, seed = 1)

# Hand it to synthpop for utility, disclosure and inference.
synthpop::compare(as_synds(sds), iris)
summary(synthpop::lm.synds(Sepal.Length ~ Petal.Length, as_synds(sds)))
}
}
\seealso{
\code{\link[=as_synds]{as_synds()}} for the synthpop toolchain, \code{\link[=syn.tabfound]{syn.tabfound()}} to
let synthpop drive instead, \code{\link[=tabfound_synthetic]{tabfound_synthetic()}} to pull the frames
out, and \code{\link[=tabfound_impute]{tabfound_impute()}} for the imputation sibling.
}
