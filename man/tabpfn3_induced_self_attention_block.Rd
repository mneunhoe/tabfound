% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn3.R
\name{tabpfn3_induced_self_attention_block}
\alias{tabpfn3_induced_self_attention_block}
\title{Induced self-attention over a column's cells}
\usage{
tabpfn3_induced_self_attention_block(
  emsize,
  nhead,
  num_inducing_points,
  dim_feedforward,
  softmax_scaling_layer = NULL
)
}
\description{
Attention between every pair of rows would cost \code{O(n^2)}. Instead a
fixed set of learned inducing vectors attends to the training rows,
and every row -- training and test alike -- attends back to that
summary. Two cross-attentions, both linear in the row count.
}
\details{
The summary is a function of the training rows alone, which is what
makes it cacheable: \code{cached_hidden} skips the first half entirely.
}
\keyword{internal}
