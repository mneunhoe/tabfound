% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-registry.R
\name{register_backend}
\alias{register_backend}
\title{Register a model backend}
\usage{
register_backend(
  name,
  build,
  translate_key = identity,
  detect = function(config) FALSE,
  task_of = function(config) NULL,
  subfolder_for = function(task) NULL,
  classifier = NULL,
  regressor = NULL,
  describe = NULL,
  peak_terms = NULL,
  aliases = character(),
  description = "",
  kv_cache_capable = FALSE,
  handles_missing = FALSE,
  imputes_internally = FALSE,
  parity = NA_character_
)
}
\arguments{
\item{name}{Character. Unique backend id, e.g. \code{"tabpfn"}.}

\item{build}{Function \verb{(config, task)} returning a constructed
\code{nn_module} with randomly initialised weights.}

\item{translate_key}{Function mapping a checkpoint key to the R
\code{named_parameters()} path. Defaults to identity.}

\item{detect}{Function \code{(config)} returning \code{TRUE} when this backend
recognises the config. Used to infer the backend when the caller
does not name one.}

\item{task_of}{Function \code{(config)} returning \code{"classification"} or
\code{"regression"}.}

\item{subfolder_for}{Function \code{(task)} returning the artifact
subfolder for a task, or \code{NULL}.}

\item{classifier, regressor}{Functions \verb{(ctx, ...)} that take the
loaded-model context (see \code{\link[=load_backend_model]{load_backend_model()}}) and return the
user-facing predictor object.}

\item{describe}{Optional function \verb{(config, task)} returning
\code{list(title, subtitle, facts, symbols, stages)}, where \code{stages} is a
list of \code{\link[=arch_stage]{arch_stage()}}s. This is what \code{\link[=tabfound_architecture]{tabfound_architecture()}} and
\code{\link[=plot_architecture]{plot_architecture()}} draw; a backend without one cannot be
diagrammed.}

\item{peak_terms}{Optional function
\verb{(n_context, n_query, n_features, opts, config)} returning the shapes
that decide peak memory, in tensor \emph{elements} rather than bytes:
\code{list(stages = list(list(name, act, att), ...), persistent, n_estimators)}.
The core turns those into bytes with fitted per-backend constants --
see \code{\link[=estimate_peak_memory]{estimate_peak_memory()}} and \verb{inst/memory/}. \code{act} is a stage's
largest activation, \code{att} the attention scores it materialises; the
peak is the maximum over stages, never the sum, because only one is
live at a time. A backend without this cannot be preflighted.}

\item{aliases}{Named character vector mapping user-friendly model
shorthands to local paths or Hub repo ids.}

\item{description}{One-line human description.}

\item{kv_cache_capable}{Logical. Can this backend hand back a
conditioned state for the training rows that a later \code{predict()} can
start from? That is what \code{\link[=tabfound_cache]{tabfound_cache()}} persists. An
architecture that re-fits statistics over train and test together
has nothing to hand over.}

\item{handles_missing}{Logical. Does the \strong{network} tolerate
\code{NA}/\code{NaN} in the predictors? TabPFN encodes them explicitly, as an
is-missing channel beside the value, and TabFM maps them to a
sentinel. TabICL propagates them to \code{NaN} output and Mitra silently
collapses the column, so for those it is \code{FALSE}.}

\item{imputes_internally}{Logical. Does the \strong{predictor} fill them in
before the network sees them? TabICL, Mitra and TabFM all run their
reference wrapper's mean imputer as the first preprocessing step.

The two flags used to be one, which conflated guarantees that are
not the same. Either means \code{\link[=tabfound]{tabfound()}} need not impute, so
\code{na_action = "auto"} reads their disjunction; but a network that
\emph{conditions on} missingness treats it as information, while a
wrapper that mean-fills has thrown that information away before the
model is reached. For imputation and synthesis, where the
missingness pattern is the object of study, that difference is the
whole question -- see \code{\link[=tabfound_impute]{tabfound_impute()}}.}

\item{parity}{Optional character. Reference Python package + version
this backend has been checked against.}
}
\value{
Invisibly, the backend spec.
}
\description{
Register a model backend
}
