% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn3.R
\name{tabpfn3_softmax_scaling_mlp}
\alias{tabpfn3_softmax_scaling_mlp}
\title{Query-aware attention temperature}
\usage{
tabpfn3_softmax_scaling_mlp(num_heads, head_dim, n_hidden = 64L)
}
\arguments{
\item{num_heads, head_dim}{Shape of the queries this scales.}

\item{n_hidden}{Width of both MLPs' hidden layer.}
}
\description{
Softmax attention sharpens as the key sequence grows; v3 learns the
correction rather than fixing it at \code{1/sqrt(d)}. Queries are scaled by
}
\details{
\verb{base_mlp(log n) * (1 + tanh(query_mlp(q)))}

where \code{n} is the number of keys, so the first factor is a per-head,
per-channel function of context length and the second is a
per-query modulation of it. The usual \code{1/sqrt(d)} is still applied
afterwards by the attention itself.
}
\keyword{internal}
