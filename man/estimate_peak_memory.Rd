% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-memory.R
\name{estimate_peak_memory}
\alias{estimate_peak_memory}
\title{Estimate peak memory for a fit / predict pair, before allocating}
\usage{
estimate_peak_memory(
  model,
  n_context,
  n_query = 0L,
  n_features,
  ...,
  backend = NULL,
  device = NULL,
  task = NULL,
  available = NULL,
  total = NULL
)
}
\arguments{
\item{model}{A \code{tabfound_model}, an artifact directory, a model id
with artifacts already downloaded, or a \code{config.json} read into a
list.}

\item{n_context}{Training rows the model will be fitted on.}

\item{n_query}{Rows to predict. Only the largest chunk is ever
resident, so a \code{predict_chunk_size} below this caps the transient
term.}

\item{n_features}{Predictor columns.}

\item{...}{Backend knobs that change the peak, overriding both the
predictor's defaults and (for a model object) the arguments it was
built with: \code{n_estimators}, \code{kv_cache}, \code{save_peak_memory_factor},
\code{predict_chunk_size}.}

\item{backend}{Optional backend name, when detection from the config
is ambiguous.}

\item{device}{Device the run will use. Constants are per-device;
only \code{"cpu"} ships today.}

\item{task}{\code{"classification"} or \code{"regression"}, when \code{model} is a
config that does not say.}

\item{available, total}{Override the machine probe, in bytes. Mostly
for asking "would this fit on a 16 GB laptop?" and for tests.}
}
\value{
An object of class \code{tabfound_memory_estimate}: a list with
\code{weights_bytes}, \code{persistent_bytes}, \code{transient_bytes},
\code{total_peak_bytes}, \code{system_total_bytes}, \code{system_avail_bytes},
\code{verdict} (\code{"ok"}, \code{"tight"}, \code{"exceeds"}, or \code{"unknown"} when the
machine could not be probed) and \code{suggestions}.
}
\description{
Predicts the high-water mark of a \code{fit()} + \code{predict()} cycle at given
dimensions and compares it with what the machine has free \emph{now}.
Nothing here builds a tensor, and nothing here needs the weights: an
artifact directory or the contents of a \code{config.json} is enough, so
the check can run before a multi-gigabyte download.
}
\details{
The answer is a breakdown, because which term dominates decides what
to do about it:
\itemize{
\item \code{weights_bytes} -- the checkpoint, resident for the object's life.
Fixed. Only a smaller model changes it.
\item \code{persistent_bytes} -- the fitted context and, when \code{kv_cache = TRUE},
one cache per ensemble member held for the whole \code{predict()} call.
\code{kv_cache} does not save memory; it \emph{moves} it here from the
transient term, and multiplies it by \code{n_estimators}.
\item \code{transient_bytes} -- the forward pass. The largest single stage, not
the sum of them, and counted once for the whole ensemble: members
run sequentially, so \code{n_estimators} does not multiply this.
}
}
\section{Accuracy}{

The shipped constants are anchored to observed working and failing
runs rather than fitted to a measurement grid -- see
\code{inst/memory/README.md} and the \code{calibration} field of the result. They
are set to err high: for a guard, a false "tight" costs a warning and
a false "ok" costs the session.
}

\examples{
\dontrun{
# Before downloading anything, from a config you already have:
estimate_peak_memory(jsonlite::fromJSON("config.json"),
                     n_context = 6426, n_query = 714, n_features = 90)

# Would it fit on a 16 GB laptop with half of it free?
estimate_peak_memory(clf, 6426, 714, 90, available = 8e9)
}
}
\seealso{
\code{\link[=list_models]{list_models()}} for what a download would cost on disk.
}
