% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{fit_minmax_scaler}
\alias{fit_minmax_scaler}
\alias{apply_minmax_scaler}
\alias{invert_minmax_scaler}
\title{Fit a min-max target scaler}
\usage{
fit_minmax_scaler(y)

apply_minmax_scaler(y, scaler)

invert_minmax_scaler(y, scaler)
}
\arguments{
\item{y}{Numeric vector.}

\item{scaler}{Result of \code{\link[=fit_minmax_scaler]{fit_minmax_scaler()}}.}
}
\value{
A list with \code{min} and \code{range}.
}
\description{
Mitra's regressor is trained on targets mapped to \verb{[0, 1]} by
\code{(y - min) / (max - min)}, not standardized — its preprocessor calls
this \code{normalize_y} and inverts it on the way out. Different convention
from TabFM and TabICL, hence a second scaler rather than a flag.
}
\keyword{internal}
