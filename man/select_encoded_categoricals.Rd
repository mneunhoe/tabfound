% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-pipelines.R
\name{select_encoded_categoricals}
\alias{select_encoded_categoricals}
\title{Which categorical columns an encoder actually touches}
\usage{
select_encoded_categoricals(X_train, cat_ix, name)
}
\arguments{
\item{X_train}{Numeric matrix, used for the level counts.}

\item{cat_ix}{Integer vector of 1-based categorical columns.}

\item{name}{The member's \code{categorical_name}.}
}
\value{
The subset of \code{cat_ix} the encoder will act on.
}
\description{
The \code{ordinal} family comes in three widths. Plain \code{ordinal} encodes
every categorical column. The \verb{_common_categories} variants first drop
columns whose rarest level is thin, on the grounds that a level seen
fewer than ten times cannot teach the model anything and its code would
just be noise; \verb{_very_common_categories} additionally drops columns
with too many levels for the row count.
}
\keyword{internal}
