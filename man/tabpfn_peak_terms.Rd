% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn.R
\name{tabpfn_peak_terms}
\alias{tabpfn_peak_terms}
\title{TabPFN v2's peak-memory shapes}
\usage{
tabpfn_peak_terms(n_context, n_query, n_features, opts, config)
}
\arguments{
\item{n_context, n_query, n_features}{Dimensions; \code{n_query} should
already be the resident chunk (see \code{\link[=.resident_query]{.resident_query()}}).}

\item{opts}{Resolved predictor arguments.}

\item{config}{The model config.}
}
\description{
A per-feature transformer: the state is \verb{rows x feature-groups x embedding_dim}, with three columns to a group and the target riding
along as one more group. Every layer attends twice -- across rows for
each feature group, then across feature groups for each row -- so the
two sublayers are separate stages with quite different score shapes.
}
\details{
Both v2.5 and v2.6 use this; \code{save_peak_memory_factor}, which v2.6
honours, is applied by the core rather than here, since it divides
whatever the stages turn out to be.
}
\keyword{internal}
