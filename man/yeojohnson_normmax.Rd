% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{yeojohnson_normmax}
\alias{yeojohnson_normmax}
\title{Maximum-likelihood Yeo-Johnson lambda}
\usage{
yeojohnson_normmax(x)
}
\arguments{
\item{x}{Numeric vector, missing values already dropped.}
}
\description{
\code{scipy.stats.yeojohnson_normmax(x)} with \code{brack = NULL}, which is what
\code{PowerTransformer} calls. The search bounds are derived from the data
rather than fixed: they are the lambdas at which the transform of the
largest observed value would over- or underflow, which is the widest
interval where the objective is even computable.
}
\details{
This is the one transform in this file that is not bit-exact against
the reference, and the reason is worth stating precisely. The
likelihood agrees to about one ULP — the log-space variance goes
through a \code{logsumexp} whose summation order R cannot be made to share.
Brent's accept/reject test is a strict comparison, so a last-bit
difference occasionally sends the two searches down different final
steps. They then stop within \code{xatol} of each other, which is
\code{1.48e-8}: measured, the lambdas differ by up to ~2e-8 and the
transformed values by ~8e-8. That is an order of magnitude inside the
\verb{preprocess:*} parity tolerance, and it is a property of the objective
being flat at its optimum rather than of either answer being wrong.
}
\keyword{internal}
