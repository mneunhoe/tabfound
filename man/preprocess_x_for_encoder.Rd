% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-tabpfn-inputprep.R
\name{preprocess_x_for_encoder}
\alias{preprocess_x_for_encoder}
\title{Preprocess X and produce main + nan_indicators tensors for the X encoder}
\usage{
preprocess_x_for_encoder(x, single_eval_pos, state = NULL)
}
\arguments{
\item{x}{Tensor of shape \verb{(S, BG, g)}.}

\item{single_eval_pos}{Integer number of training rows.}

\item{state}{Fitted statistics from an earlier call, or \code{NULL} to fit.}
}
\value{
A list with \verb{$main}, \verb{$nan_indicators} (each \verb{(S, BG, g)}) and
\verb{$state}.
}
\description{
Matches the actual \code{nn.Sequential} of the
\code{Prior-Labs/tabpfn_2_5} default classifier ckpt:
}
\details{
\enumerate{
\item RemoveEmpty                  (no-op for non-padded inputs with B=1)
\item NaN handling                 (impute with train mean + signed indicator)
\item NormalizeFeatureGroups #1    (CONFIGURED OFF in this ckpt — no-op)
\item FeatureTransform             (z-normalize only — \code{remove_outliers=False})
\item NormalizeFeatureGroups #2    (active — scale non-const cols by sqrt(g/n_used))
}

If you load a different ckpt with different flags, the
configuration must be threaded through here — see
\code{inst/python/dump_py_forward.py} for how to introspect them.

Every statistic this fits comes from the training rows alone -- the
imputation mean, the z-normalisation mean and standard deviation, and
the per-group non-constant mask. Nothing here looks at the test rows,
which is what makes a v2.5 KV cache exactly equivalent to a full
forward rather than approximately so. Pass \code{state} to reuse them on a
batch that has no training rows in it.
}
\keyword{internal}
