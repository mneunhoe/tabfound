% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-transforms.R
\name{fit_quantile_transformer}
\alias{fit_quantile_transformer}
\title{Fit a per-column QuantileTransformer matching sklearn's uniform output.}
\usage{
fit_quantile_transformer(
  X_train,
  n_quantiles,
  output_distribution = "uniform",
  extrapolate_ratio = NULL
)
}
\arguments{
\item{extrapolate_ratio}{\code{NULL} for the ordinary clipping transform, or
a non-negative number to extrapolate past \verb{[0, 1]} at transform time.
Nothing extra is fitted for it: the training range it extrapolates
from is \code{quantiles_[1, ]} and \code{quantiles_[n, ]}, which are the 0th and
100th percentiles and so already the per-column min and max. The
reference's two implementations read it both ways -- sklearn stores
\code{nanmin}/\code{nanmax}, the torch one takes the outer breakpoints -- and
they are the same numbers.}
}
\description{
Fit a per-column QuantileTransformer matching sklearn's uniform output.
}
\keyword{internal}
