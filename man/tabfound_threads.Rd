% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-utils.R
\name{tabfound_threads}
\alias{tabfound_threads}
\title{Set libtorch's thread count}
\usage{
tabfound_threads(n = NULL, interop = NULL)
}
\arguments{
\item{n}{Number of intra-op threads. \code{NULL} reports the current
setting without changing it.}

\item{interop}{Number of inter-op threads, or \code{NULL} to leave it.}
}
\value{
Invisibly, the intra-op thread count in effect afterwards.
}
\description{
libtorch defaults to one intra-op thread per core, which is right for
a single model in a single process and catastrophic inside a
\code{parallel} / \code{future} worker pool: \code{k} workers each spawning \code{n_cores}
threads oversubscribe the machine by \code{k}-fold, and the run gets slower
the more workers you add.
}
\details{
This is the knob for that. The rule of thumb for \code{k} R workers is
\code{tabfound_threads(max(1, parallel::detectCores() \%/\% k))}, called
inside each worker -- the setting is per process.

The other half of the advice is not to fork at all: multiple-imputation
chains and ensemble members are already sequential calls into a
multi-threaded library, so the parallelism is better left to torch
than taken from it. Forking a process that has already initialised
libtorch is its own hazard -- the child inherits a thread pool it
cannot use -- so a worker pool must be created \emph{before} the first
torch call, or with \code{future::plan(multisession)} rather than
\code{multicore}.

Set it \strong{before the first forward pass}. libtorch's native backend
refuses both counts once its parallel region has started, and says so
on stderr from C++ rather than through an R condition, so a late call
looks as though it worked. Reading back the value is the check.
}
\examples{
\dontrun{
tabfound_threads()        # report
tabfound_threads(4L)      # four intra-op threads in this process
}
}
