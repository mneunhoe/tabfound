% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep-sklearn.R
\name{.nanquantile}
\alias{.nanquantile}
\title{\code{np.nanpercentile(x, p)} with NumPy's exact arithmetic}
\usage{
.nanquantile(x, p)
}
\description{
Nominally this is R's \code{quantile(type = 7)}, and on most data the two
agree to the last bit. They are not the same expression, though:
NumPy's \code{lerp} interpolates up from the lower point while the weight
is below 0.5 and \emph{down from the upper point} once it is above, where R
always writes \code{(1-h)*lo + h*hi}.
}
\details{
A 1-ULP disagreement would normally be beneath notice. It is not here.
\code{QuantileTransformer} fitted with \code{n_quantiles = n_samples} produces
knots that are supposed to \emph{be} the sorted data, and the transform then
tests \code{q <= x} against them. One knot a single ULP above its own data
value flips that test, and the value's rank — and hence its transformed
output — moves by a whole step.

\code{p} is a \strong{percentage}, as \code{np.nanpercentile} takes it, and the
division by 100 happens here — deliberately. sklearn passes
\code{references_ * 100} and NumPy divides it straight back, and that round
trip is not the identity in floating point: \code{10/59 * 100 / 100} misses
\code{10/59} by an ULP, which is enough to turn an exact knot index into an
interpolation between two neighbours. Doing the multiply outside and
the divide inside is what reproduces it.
}
\keyword{internal}
